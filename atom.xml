<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>我的博客</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://zanderchang.github.io/"/>
  <updated>2019-05-15T05:17:12.804Z</updated>
  <id>http://zanderchang.github.io/</id>
  
  <author>
    <name>Glory</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>秘密共享及门限签名</title>
    <link href="http://zanderchang.github.io/2019/05/14/%E7%A7%98%E5%AF%86%E5%85%B1%E4%BA%AB%E5%8F%8A%E9%97%A8%E9%99%90%E7%AD%BE%E5%90%8D/"/>
    <id>http://zanderchang.github.io/2019/05/14/秘密共享及门限签名/</id>
    <published>2019-05-14T11:07:13.000Z</published>
    <updated>2019-05-15T05:17:12.804Z</updated>
    
    <content type="html"><![CDATA[<p>Elgamal 签名算法 $\rightarrow$ Harn 的门限签名方案</p><p>Asmuth-Bloom 法</p><a id="more"></a><h1 id="Elgamal-签名算法"><a href="#Elgamal-签名算法" class="headerlink" title="Elgamal 签名算法"></a>Elgamal 签名算法</h1><p>依赖有限域上的离散指数的难计算性，即设$g$为素数$p$的模循环群的原根，对任意的$a$，计算：</p><p>$$b=g^a\bmod p$$</p><p>是容易的（通过幂取模算法）。反过来，给定$b$计算满足上式的$a$是非常困难的，$a$称为以$g$为基$b$的离散对数。</p><h2 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h2><p>Alice随机选择大素数$p$和$1$到$p$之间的整数$x$，找出有限域$Z_p$的一个生成元$g$，计算：</p><p>$$y=g^x\bmod p$$</p><p>则公钥为$y$，私钥为$x$。</p><h2 id="签名"><a href="#签名" class="headerlink" title="签名"></a>签名</h2><p>设待签名消息为$m$，Alice随机选择整数$k$，满足$k&lt;p$，$gcd(k,p-1)=1$。计算：</p><p>$$r=g^k\bmod p$$<br>$$s=(m-xr)k^{-1}\bmod p$$</p><p>将$m$的签名$(r,s)$发送给Bob。</p><h2 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h2><p>Bob收到$(r,s)$，利用Alice的公钥$y$验证等式：</p><p>$$g^m\equiv y^rr^s\bmod p$$</p><p>若等式成立则签名有效。</p><h1 id="Harn-的门限签名方案"><a href="#Harn-的门限签名方案" class="headerlink" title="Harn 的门限签名方案"></a>Harn 的门限签名方案</h1><h2 id="初始化-1"><a href="#初始化-1" class="headerlink" title="初始化"></a>初始化</h2><p>假设有$n$个成员$P_1,P_2,…P_n$，$t$为门限值，$p$、$q$为大素数，$q|(p-1)$，$g$为有限域$Z_p$上的$q$阶生成元，$h$是$g$的循环群中的一个数，待签名消息为$m$，每个成员随机选择$z_i$和$x_i$，计算：</p><p>$$y_i=g^{z_i}\bmod p$$</p><p>成员$P_i$私钥为$z_i$，组公钥$y$计算如下：</p><p>$$y=\prod_{i=1}^ny_i\bmod p$$</p><p>$P_i(i=1,2,…,n)$选取一个$t-1$次多项式：$f_i(x)=z_i+a_{i,1}x+a_{i,2}x^2+…+a_{i,t-1}x^{t-1}$，计算影子份额$f_i(x_j)\bmod q$发送给$P_j$，并计算验证信息：</p><p>$$y_{i,j}=g^{f_i(x_j)}\bmod p$$</p><h2 id="产生部分签名"><a href="#产生部分签名" class="headerlink" title="产生部分签名"></a>产生部分签名</h2><p>设参与签名的成员$P_1,P_2,…P_t$。成员$P_i(i=1,2,…,t)$选择一个随机数$k_i\in[1,q-1]$，计算$r_i=g^{k_i}\bmod p$并将其广播给所有成员。</p><p>收到其他成员广播的$r_i$后，$P_i$计算</p><p>$$r=\prod_{i=1}^tr_i\bmod p$$</p><p>通过自己的私钥$z_i$、$k_i$、份额$f_j(x_i)(j=t+1,t+2,…,n)$计算：</p><p>$$s_i=z_i+\lbrace\sum_{j=t+1}^nf_j(x_i)(\prod_{k=1,k\neq i}^t\frac{-x_k}{x_k-x_j})\rbrace m-k_i r\bmod q$$</p><p>将部分签名$(r_i,s_i)$发送给签名合成者。</p><p>合成者收到$\lbrace r_i,s_i\rbrace$后，通过下式验证其有效性：</p><p>$$\lbrace y_i(\prod_{j=t+1}^ny_{j,i})^{\prod_{k=1,k\neq i}^t\frac{-x_k}{x_i-x_k}}\rbrace^m=r_i^r g^{s_j}\bmod p$$</p><p>若验证式成立，则收到的部分签名$\lbrace r_i,s_i\rbrace$有效。</p><h2 id="产生组签名"><a href="#产生组签名" class="headerlink" title="产生组签名"></a>产生组签名</h2><p>合成者确认接收到$t$个正确的部分签名后，计算：</p><p>$$s=\sum_{i=1}^ts_i\bmod q$$</p><p>将$\lbrace r,s\rbrace$作为消息$m$的组签名。</p><h2 id="验证签名"><a href="#验证签名" class="headerlink" title="验证签名"></a>验证签名</h2><p>验证者收到组签名后，验证其有效性：</p><p>$$y^m=r^r g^s \bmod p$$</p><h1 id="Asmuth-Bloom-法"><a href="#Asmuth-Bloom-法" class="headerlink" title="Asmuth-Bloom 法"></a>Asmuth-Bloom 法</h1><h2 id="初始化-2"><a href="#初始化-2" class="headerlink" title="初始化"></a>初始化</h2><p>假设$S$为共享秘密，$DCA$为秘密分发者，有$n$个成员$P_1,P_2,…,P_n$，$t$为恢复秘密的门限值。$DC$选择大素数$q$和严格递增的序列$d=\lbrace d_1,d_2,…,d_n\rbrace$，$d$满足如下条件：</p><p>$$d_1&lt;d_2&lt;…&lt;d_n$$</p><p>$$(d_i,d_j)=1,(i\neq j)$$</p><p>$$(d_i,q)=1,(i=1,2,…,n)$$</p><p>$$\prod_{i=1}^td_i&gt;q\prod_{i=1}^{t-1}d_{n-i+1}$$</p><h2 id="秘密分发"><a href="#秘密分发" class="headerlink" title="秘密分发"></a>秘密分发</h2><p>令$M=\prod_{i=1}^td_i$，选择满足式$0\leq A\leq \frac{M}{q}-1$的整数$A$，$DC$计算：</p><p>$$Z=S+Aq$$</p><p>$$Z_i=Z\bmod d_i,i=1,2,…n$$</p><p>并将$(Z_i,d_i)$作为秘密份额发给成员$P_i,(i=1,2,…,n)$。</p><h2 id="秘密恢复"><a href="#秘密恢复" class="headerlink" title="秘密恢复"></a>秘密恢复</h2><p>选取$t$个成员$\lbrace P_1,P_2,…,P_t\rbrace$参与秘密的恢复，通过交换秘密份额，任意成员$P_i$可以建立同余方程组：</p><p>$$<br>\begin{cases}<br>Z_1=Z\bmod d_1 \<br>Z_2=Z\bmod d_2 \<br>\vdots \<br>Z_t=Z\bmod d_t \<br>\end{cases}<br>$$</p><p>由$CRT$可知该方程组有唯一解：</p><p>$$Z=\sum_{i=1}^t\frac{D}{d_i}\cdot e_i\cdot Z_i\bmod D,i=1,2,..,t$$</p><p>其中：</p><p>$$D=\prod_{i=1}^td_i$$</p><p>$$\frac{D}{d_i}\cdot e_i \equiv 1\bmod d_i,i=1,2,…,t$$</p><p>可求出共享秘密$S=Z\bmod q$。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Elgamal 签名算法 $\rightarrow$ Harn 的门限签名方案&lt;/p&gt;
&lt;p&gt;Asmuth-Bloom 法&lt;/p&gt;
    
    </summary>
    
      <category term="密码学基础" scheme="http://zanderchang.github.io/categories/%E5%AF%86%E7%A0%81%E5%AD%A6%E5%9F%BA%E7%A1%80/"/>
    
    
      <category term="密码学" scheme="http://zanderchang.github.io/tags/%E5%AF%86%E7%A0%81%E5%AD%A6/"/>
    
      <category term="数字签名" scheme="http://zanderchang.github.io/tags/%E6%95%B0%E5%AD%97%E7%AD%BE%E5%90%8D/"/>
    
  </entry>
  
  <entry>
    <title>破解 PS4 - 第一部分：PS4 安全和用户态 ROP 简介</title>
    <link href="http://zanderchang.github.io/2019/05/06/%E7%A0%B4%E8%A7%A3-PS4-%E7%AC%AC%E4%B8%80%E9%83%A8%E5%88%86/"/>
    <id>http://zanderchang.github.io/2019/05/06/破解-PS4-第一部分/</id>
    <published>2019-05-06T02:56:54.000Z</published>
    <updated>2019-05-06T08:05:20.419Z</updated>
    
    <content type="html"><![CDATA[<p><em>原文标题 Hacking the PS4, part 1 Introduction to PS4’s security, and userland ROP (日期未知)</em></p><p>作者：<a href="https://github.com/CTurt" target="_blank" rel="noopener">CTurt</a></p><p>由于目前很长时间没有关于 PS4 破解的重大公告，我想解释一下 PS4 破解已经走了多远，以及阻止它进一步发展的原因。</p><p>我将解释一些通用于所有现代系统的安全概念，以及我在 PS4 上运行 ROP 测试所做的发现。</p><p>本系列文章的目标是展示完整的漏洞利用链，从访问 Internet 浏览器的网页，到最终在 PS4 上实现内核代码执行。</p><p>如果您对漏洞挖掘不是特别熟悉，那么您应该先阅读我的关于<a href="http://cturt.github.io/DS-exploit-finding.html" target="_blank" rel="noopener">通过保存文件中的栈粉碎漏洞来破解 DS 游戏</a>的文章。</p><p>您可以在此下载我的<a href="https://github.com/CTurt/PS4-playground" target="_blank" rel="noopener">完整设置</a>以自行运行这些测试；它目前仅适用于固件 1.76。如果您正在使用较旧的固件并希望更新到 1.76，则可以下载 <a href="http://www.psdevwiki.com/ps4/1.760.000" target="_blank" rel="noopener">1.76 PUP</a> 文件并<a href="https://www.playstation.com/en-us/support/system-updates/ps4/" target="_blank" rel="noopener">通过 USB 更新</a>。</p><a id="more"></a><h1 id="PS4-背景"><a href="#PS4-背景" class="headerlink" title="PS4 背景"></a>PS4 背景</h1><blockquote><p>您可能知道 PS4 使用的是定制的 AMD x86-64 CPU（8核），即使此特定版本可能与已知标准稍有不同，目前仍有许多研究成果可用于此 CPU 架构。 例如，PFLA（Page Fault Liberation Army）在 29C3 大会期间发布了仅使用页错误和 x86 MMU 实现完整图灵机的概念证明，您可以在 YouTube 上查看他们的精彩视频。 如果您尝试在虚拟机中运行代码并希望在主机 CPU 上执行指令，也会很有趣。</p><p>– <cite><a href="http://www.eurasia.nu/modules.php?name=News&amp;file=article&amp;sid=3251" target="_blank" rel="noopener">EurAsia 新闻 3251 号文章</a></cite></p></blockquote><p>除了表现良好的 CPU 架构外，<a href="https://en.wikipedia.org/wiki/PlayStation_4_system_software" target="_blank" rel="noopener">PS4 中使用的大部分软件</a>都是<a href="http://www.scei.co.jp/ps4-license/" target="_blank" rel="noopener">开源</a>的。</p><p>最值得注意的是，PS4 的 Orbis OS 基于 FreeBSD 9.0，就像 PS3 的操作系统一样（也有部分 NetBSD）；并包括各种许多其他开源软件，如 <a href="http://www.mono-project.com/docs/advanced/runtime/" target="_blank" rel="noopener">Mono VM</a> 和 <a href="https://www.webkit.org/" target="_blank" rel="noopener">WebKit</a>。</p><h1 id="从-WebKit-入手"><a href="#从-WebKit-入手" class="headerlink" title="从 WebKit 入手"></a>从 WebKit 入手</h1><p>WebKit 是开源的布局引擎，它被用于 iOS，Wii U，3DS，PS Vita 和 PS4 的浏览器中来呈现网页。</p><p>尽管 WebKit 被广泛使用，但它确实存在漏洞；通过阅读 <a href="https://en.wikipedia.org/wiki/Pwn2Own#Summary_of_successful_exploits" target="_blank" rel="noopener">Pwn2Own write-ups</a>，你可以了解其中的许多内容。</p><p>特别是 PS4 1.76 版本固件中的浏览器使用的 WebKit 版本易受到 <a href="https://www.exploit-db.com/exploits/28081/" target="_blank" rel="noopener">CVE-2012-3748</a> 的攻击，这是由 <code>JSArray :: sort(...)</code> 方法中基于堆的缓冲区溢出造成的。</p><p>2014年，NAS 和 Proxima <a href="http://wololo.net/talk/viewtopic.php?p=368577" target="_blank" rel="noopener">宣布</a>他们已经成功地将一个利用该漏洞破解 Mac OS X Safari 代码移植到 PS4 的浏览器，并公布了作为破解 PS4 第一个入口点的 PoC 代码。</p><p>这使我们可以任意读写 WebKit 进程可以读取和写入的内容，这些内容可以用于转储模块，并覆盖栈上的返回地址，让我们控制指令指针寄存器（<code>rip</code>）来实现 RO P执行。</p><p>从那时起，WebKit 中<a href="http://webkitgtk.org/security/WSA-2016-0003.html" target="_blank" rel="noopener">发现</a>了许多<a href="http://webkitgtk.org/security/WSA-2016-0002.html" target="_blank" rel="noopener">其他漏洞</a>，这些漏洞可能被用作 PS4 后期固件的入口点，但截至撰写时，没有人公开宣称将任何这些漏洞利用代码移植到 PS4。</p><p>如果您从未登录 PSN，您的 PS4 将无法打开 Internet 浏览器，但您可以转到“设置”，然后选择“用户指南”打开功能有限的 Web 浏览器视图，您可以用代理控制其中的内容。</p><h1 id="ROP-是什么？"><a href="#ROP-是什么？" class="headerlink" title="ROP 是什么？"></a>ROP 是什么？</h1><p>与 DS 等早期设备不同，PS4 有一个内核来控制内存不同区域的属性。其中标记为可执行的内存页面不能被覆写，并且标记为可写的内存页面不能被执行; 这称为<a href="https://en.wikipedia.org/wiki/Data_Execution_Prevention" target="_blank" rel="noopener">数据执行保护（DEP）</a>。</p><p>这意味着我们不能只将有效负载复制到内存中并执行。但是，我们可以执行已加载到内存中并标记为可执行的代码。</p><p>如果我们不能将自己的代码编写到某个地址地址，那么跳转到该地址并不是非常有用，因此我们使用 ROP。</p><p><a href="https://en.wikipedia.org/wiki/Return-oriented_programming" target="_blank" rel="noopener">ROP（Return-Oriented Programming）</a>是传统栈粉碎技术的扩展，我们可以将许多不同的地址（称为 gadgets）链接在一起，而不是仅覆盖 <code>rip</code> 将跳转到的单个值。</p><p>一条 gadget 通常只是一个所需的指令以及一个 <code>ret</code>。</p><p>在 x86_64 程序集中，当到达 <code>ret</code> 指令时，程序会从栈中弹出一个 64 位的值并且将 <code>rip</code> 跳转到它；由于我们可以控制堆栈，所以我们可以使每个 <code>ret</code> 指令跳转到下一个所需的 gadget。</p><p>例如 <code>0x80000</code> 处：</p><pre><code>mov rax, 0ret</code></pre><p><code>0x90000</code> 处：</p><pre><code>mov rbx, 0ret</code></pre><p>If we overwrite a return address on the stack to contain <code>0x80000</code> followed by <code>0x90000</code>, then as soon as the first <code>ret</code> instruction is reached execution will jump to <code>mov rax, 0</code>, and immediately afterwards, the next <code>ret</code> instruction will pop <code>0x90000</code> off the stack and jump to <code>mov rbx, 0</code>.</p><p>Effectively this chain will set both <code>rax</code> and <code>rbx</code> to 0, just as if we had written the code into a single location and executed it from there.</p><p>ROP chains aren’t just limited to a list of addresses though; assuming that from <code>0xa0000</code> contains these instructions:</p><pre><code>pop raxret</code></pre><p>We can set the first item in the chain to <code>0xa0000</code> and the next item to any desired value for <code>rax</code>.</p><p>Gadgets also don’t have to end in a <code>ret</code> instruction; we can use gadgets ending in a <code>jmp</code>:</p><pre><code>add rax, 8jmp rcx</code></pre><p>By making <code>rcx</code> point to a <code>ret</code> instruction, the chain will continue as normal:</p><pre><code>chain.add(&quot;pop rcx&quot;, &quot;ret&quot;);chain.add(&quot;add rax, 8; jmp rcx&quot;);</code></pre><p>Sometimes you won’t be able to find the exact gadget that you need on its own, but with other instructions after it. For example, if you want to set <code>r8</code> to something, but only have this gadget, you will have to set <code>r9</code> to some dummy value:</p><pre><code>pop r8pop r9ret</code></pre><p>Although you may have to be creative with how you write ROP chains, it is generally accepted that within a sufficiently large enough code dump, there will be enough gadgets for <a href="https://en.wikipedia.org/wiki/Turing_completeness" target="_blank" rel="noopener">Turing-complete</a> functionality; this makes ROP a viable method of defeating DEP.</p><h1 id="寻找-gadgets"><a href="#寻找-gadgets" class="headerlink" title="寻找 gadgets"></a>寻找 gadgets</h1><p>Think of ROP as writing a new chapter to a book, using only words that have appeared at the end of sentences in the previous chapters.</p><p>It’s obvious from the structure of most sentences that we probably won’t be able to find words like ‘and’ or ‘but’ appearing at the end of any sentences, but we will need these connectives in order to write anything meaningful.</p><p>It is quite possible however, that a sentence has ended with ‘sand’. Although the author only ever intended for the word to be read from the ‘s’, if we start reading from the ‘a’, it will appear as an entirely different word by coincidence, ‘and’.</p><p>These principles also apply to ROP.</p><p>Since almost all functions are structured with a prologue and epilogue:</p><pre><code>; Save registerspush    rbpmov     rbp, rsppush    r15push    r14push    r13push    r12push    rbxsub     rsp, 18h; Function body; Restore registersadd     rsp, 18hpop     rbxpop     r12pop     r13pop     r14pop     r15pop     rbpret</code></pre><p>You’d expect to only be able to find <code>pop</code> gadgets, or more rarely, something like <code>xor rax, rax</code> to set the return value to 0 before returning.</p><p>Having a comparison like:</p><pre><code>cmp [rax], r12ret</code></pre><p>Wouldn’t make any sense since the result of the comparison isn’t used by the function. However, there is still a possibility that we can find gadgets like these.</p><p>x86_64 instructions are similar to words in that they have variable lengths, and can mean something entirely different depending on where decoding starts.</p><blockquote><p>The x86_64 architecture is a variable-length CISC instruction set. Return-oriented programming on the x86_64 takes advantage of the fact that the instruction set is very “dense”, that is, any random sequence of bytes is likely to be interpretable as some valid set of x86_64 instructions.</p><p>– <cite><a href="https://en.wikipedia.org/wiki/Return-oriented_programming" target="_blank" rel="noopener">Wikipedia</a></cite></p></blockquote><p> To demonstrate this, take a look at the end of this function from the WebKit module:</p><pre><code>000000000052BE0D                 mov     eax, [rdx+8]000000000052BE10                 mov     [rsi+10h], eax000000000052BE13                 or      byte ptr [rsi+39h], 20h000000000052BE17                 ret</code></pre><p>Now take a look at what the code looks like if we start decoding from <code>0x52be14</code>:</p><pre><code>000000000052BE14                 cmp     [rax], r12000000000052BE17                 ret</code></pre><p>Even though this code was never intended to be executed, it is within an area of memory which has been marked as executable, so it is perfectly valid to use as a gadget.</p><p>Of course, it would be incredibily time consuming to look at every possible way of interpreting code before every single <code>ret</code> instruction manually; and that’s why tools exist to do this for you. The one which I use to search for ROP gadgets is <a href="https://github.com/0vercl0k/rp/" target="_blank" rel="noopener">rp++</a>; to generate a text file filled with gadgets, just use:</p><pre><code>rp-win-x64 -f mod14.bin --raw=x64 --rop=1 --unique &gt; mod14.txt</code></pre><h1 id="General-protection-faults"><a href="#General-protection-faults" class="headerlink" title="General protection faults"></a>General protection faults</h1><p>If we _do_ perform an access violation, such as by trying to execute a non-executable page of memory, or by trying to write to a non-writable page of memory, a general protection fault, or more specifically in this instance, a <a href="https://en.wikipedia.org/wiki/Segmentation_fault" target="_blank" rel="noopener">segmentation fault</a>, will occur.</p><p>For example, trying to execute code on the stack, which is mapped as read and write only:</p><pre><code>setU8to(chain.data + 0, 0xeb);setU8to(chain.data + 1, 0xfe);chain.add(chain.data);</code></pre><p>And trying to write to code, which is mapped as read and execute only:</p><pre><code>setU8to(moduleBases[webkit], 0);</code></pre><p>If a general protection fault occurs, a message saying “There is not enough free system memory” will appear, and the page will fail to load:</p><p><img src="/img/hacking-ps4-1/segfault.png" alt></p><p>This message will also be displayed for other hard faults, such as division by 0, or execution of an invalid instruction or unimplemented system call, but most commonly it will be encountered by performing a segmentation fault.</p><h1 id="ASLR"><a href="#ASLR" class="headerlink" title="ASLR"></a>ASLR</h1><p><a href="https://en.wikipedia.org/wiki/Address_space_layout_randomization" target="_blank" rel="noopener">Address Space Layout Randomization (ASLR)</a> is a security technique which causes the base addresses of modules to be different every time you start the PS4.</p><p>It has been reported to me that <a href="https://www.youtube.com/watch?v=aEu208625XA" target="_blank" rel="noopener">very old firmwares (1.05) don’t have ASLR enabled</a>, but it was introduced sometime before firmware 1.70. Note that kernel ASLR is not enabled (for firmwares 1.76 and lower at least), which will be proved later in the article.</p><p>For most exploits ASLR would be a problem because if you don’t know the addresses of the gadgets in memory, you would have no idea what to write to the stack.</p><p>Luckily for us, we aren’t limited to just writing static ROP chains. We can use JavaScript to read the modules table, which will tell us the base addresses of all loaded modules. Using these bases, we can then calculate the addresses of all our gadgets before we trigger ROP execution, defeating ASLR.</p><p>The modules table also includes the filenames of the modules:</p><ul><li>WebProcess.self</li><li>libkernel.sprx</li><li>libSceLibcInternal.sprx</li><li>libSceSysmodule.sprx</li><li>libSceNet.sprx</li><li>libSceNetCtl.sprx</li><li>libSceIpmi.sprx</li><li>libSceMbus.sprx</li><li>libSceRegMgr.sprx</li><li>libSceRtc.sprx</li><li>libScePad.sprx</li><li>libSceVideoOut.sprx</li><li>libScePigletv2VSH.sprx</li><li>libSceOrbisCompat.sprx</li><li>libSceWebKit2.sprx</li><li>libSceSysCore.sprx</li><li>libSceSsl.sprx</li><li>libSceVideoCoreServerInterface.sprx</li><li>libSceSystemService.sprx</li><li>libSceCompositeExt.sprx</li></ul><p>Although the PS4 predominantly uses the [<strong>S</strong>igned] <strong>P</strong>PU <strong>R</strong>elocatable E<strong>x</strong>ecutable ([S]PRX) format for modules, some string references to [<strong>S</strong>igned] <strong>E</strong>xecutable and <strong>L</strong>inking <strong>F</strong>ormat ([S]ELF) object files can also be found in the <code>libSceSysmodule.sprx</code> dump, such as <code>bdj.elf</code>, <code>web_core.elf</code> and <code>orbis-jsc-compiler.self</code>. This combination of modules and objects is similar to what is used in the PSP and PS3.</p><p>You can view <a href="http://www.psdevwiki.com/ps4/Libraries" target="_blank" rel="noopener">a complete list of all modules available</a> (not just those loaded by the browser) in <code>libSceSysmodule.sprx</code>. We can load and dump some of these through several of Sony’s custom system calls, which will be explained later in this article.</p><h1 id="JuSt-ROP"><a href="#JuSt-ROP" class="headerlink" title="JuSt-ROP"></a>JuSt-ROP</h1><p>Using JavaScript to write and execute dynamic ROP chains gives us a tremendous advantage over a traditional, static buffer overflow attack.</p><p>As well as being necessary to defeat ASLR, JavaScript also lets us read the user agent of the browser, and provide different ROP chains for different browser versions, giving our exploit a greater range of compatibility.</p><p>We can even use JavaScript to read the memory at our gadgets’ addresses to check that they are correct, giving us almost perfect reliability. Theoretically, you could take this even further by writing a script to dynamically find ROP gadgets and then build ROP chains on the fly.</p><p>Writing ROP chains dynamically, rather than generating them with a script beforehand, just makes sense.</p><p>I created a JavaScript framework for writing ROP chains, <a href="https://github.com/CTurt/JuSt-ROP" target="_blank" rel="noopener">JuSt-ROP</a>, for this very reason.</p><h1 id="JavaScript-caveats"><a href="#JavaScript-caveats" class="headerlink" title="JavaScript caveats"></a>JavaScript caveats</h1><p>JavaScript represents numbers using the <a href="https://en.wikipedia.org/wiki/IEEE_floating_point" target="_blank" rel="noopener">IEEE-754</a> double-precision (64 bit) format. This provides us with 53 bit precision, meaning that it isn’t possible to represent every 64 bit value, approximations will have to be used for some.</p><p>If you just need to set a 64 bit value to something low, like <code>256</code>, then <code>setU64to</code> will be fine.</p><p>But for situations in which you need to write a buffer or struct of data, there is the possibility that certain bytes will be written incorrectly if it has been written in 64 bit chunks.</p><p>Instead, you should write data in 32 bit chunks (remembering that the PS4 is little endian), to ensure that every byte is exact.</p><h1 id="系统调用"><a href="#系统调用" class="headerlink" title="系统调用"></a>系统调用</h1><p>Interestingly, the PS4 uses the same <a href="https://www.freebsd.org/doc/en/books/developers-handbook/x86-system-calls.html#x86-alternate-calling-convention" target="_blank" rel="noopener">calling convention as Linux and MS-DOS for system calls</a>, with arguments stored in registers, rather than the traditional UNIX way (which FreeBSD uses by default), with arguments stored in the stack:</p><ul><li><code>rax</code> - System call number</li><li><code>rdi</code> - Argument 1</li><li><code>rsi</code> - Argument 2</li><li><code>rdx</code> - Argument 3</li><li><code>r10</code> - Argument 4</li><li><code>r8</code> - Argument 5</li><li><code>r9</code> - Argument 6</li></ul><p>We can try to perform any system call with the following JuSt-ROP method:</p><pre><code>this.syscall = function(name, systemCallNumber, arg1, arg2, arg3, arg4, arg5, arg6) {    console.log(&quot;syscall &quot; + name);    this.add(&quot;pop rax&quot;, systemCallNumber);    if(typeof(arg1) !== &quot;undefined&quot;) this.add(&quot;pop rdi&quot;, arg1);    if(typeof(arg2) !== &quot;undefined&quot;) this.add(&quot;pop rsi&quot;, arg2);    if(typeof(arg3) !== &quot;undefined&quot;) this.add(&quot;pop rdx&quot;, arg3);    if(typeof(arg4) !== &quot;undefined&quot;) this.add(&quot;pop rcx&quot;, arg4);    if(typeof(arg5) !== &quot;undefined&quot;) this.add(&quot;pop r8&quot;, arg5);    if(typeof(arg6) !== &quot;undefined&quot;) this.add(&quot;pop r9&quot;, arg6);    this.add(&quot;mov r10, rcx; syscall&quot;);}</code></pre><p>Just make sure to set the stack base to some free memory beforehand:</p><pre><code>this.add(&quot;pop rbp&quot;, stackBase + returnAddress + 0x1400);</code></pre><p>Using system calls can tell us a huge amount about the PS4 kernel. Not only that, but using system calls is most likely the only way that we can interact with the kernel, and thus potentially trigger a kernel exploit.</p><p>If you are reverse engineering modules to identify some of Sony’s custom system calls, you may come across an alternative calling convention:</p><p>Sometimes Sony performs system calls through regular system call 0 (which usually does nothing in FreeBSD), with the first argument (<code>rdi</code>) controlling which system call should be executed:</p><ul><li><code>rax</code> - 0</li><li><code>rdi</code> - System call number</li><li><code>rsi</code> - Argument 1</li><li><code>rdx</code> - Argument 2</li><li><code>r10</code> - Argument 3</li><li><code>r8</code> - Argument 4</li><li><code>r9</code> - Argument 5</li></ul><p>It is likely that Sony did this to have easy compatibility with the function calling convention. For example:</p><pre><code>.global syscallsyscall:    xor     rax, rax    mov     r10, rcx    syscall    ret</code></pre><p>Using this, they can perform system calls from C using the function calling convention:</p><pre><code>int syscall();int getpid(void) {    return syscall(20);}</code></pre><p>When writing ROP chains, we can use either convention:</p><pre><code>// Both will get the current process ID:chain.syscall(&quot;getpid&quot;, 20);chain.syscall(&quot;getpid&quot;, 0, 20);</code></pre><p>It’s good to be aware of this, because we can use whichever one is more convenient for the gadgets that are available.</p><h1 id="getpid"><a href="#getpid" class="headerlink" title="getpid"></a>getpid</h1><p>Just by using system call 20, <a href="http://www.freebsd.org/cgi/man.cgi?query=getpid&amp;sektion=2" target="_blank" rel="noopener">getpid(void)</a>, we can learn a lot about the kernel.</p><p>The very fact that this system call works at all tells us that Sony didn’t bother mixing up the system call numbers as a means of <a href="https://en.wikipedia.org/wiki/Security_through_obscurity" target="_blank" rel="noopener">security through obscurity</a> (under the BSD license they could have done this without releasing the new system call numbers).</p><p>So, we automatically have a <a href="http://fxr.watson.org/fxr/source/kern/syscalls.master?v=FREEBSD9" target="_blank" rel="noopener">list of system calls in the PS4 kernel</a> to try.</p><p>Secondly, by calling <code>getpid()</code>, restarting the browser, and calling it again, we get a return value 2 higher than the previous value.</p><p>This tells us that the Internet Browser app actually consists of 2 separate processes: the WebKit core (which we take over), that handles parsing HTML and CSS, decoding images, and executing JavaScript for example, and another one to handle everything else: displaying graphics, receiving controller input, managing history and bookmarks, etc.</p><p>Also, although FreeBSD has supported <a href="http://security.stackexchange.com/questions/88692/do-randomized-pid-bring-more-security" target="_blank" rel="noopener">PID randomisation</a> since 4.0, sequential PID allocation is the default behaviour.</p><p>The fact that PID allocation is set to the default behaviour indicates that Sony likely didn’t bother adding any additional security enhancements such as those encouraged by projects like <a href="https://hardenedbsd.org/" target="_blank" rel="noopener">HardenedBSD</a>, other than userland ASLR.</p><h2 id="How-many-custom-system-calls-are-there"><a href="#How-many-custom-system-calls-are-there" class="headerlink" title="How many custom system calls are there?"></a>How many custom system calls are there?</h2><p>The <a href="http://fxr.watson.org/fxr/source/kern/syscalls.master?v=FREEBSD9#L952" target="_blank" rel="noopener">last standard FreeBSD 9 system call</a> is <code>wait6</code>, number <code>532</code>; anything higher than this must be a custom Sony system call.</p><p>Invoking most of Sony’s custom system calls without the correct arguments will return error <code>0x16</code>, <code>&quot;Invalid argument&quot;</code>; however, any compatibility or unimplemented system calls will report the “There is not enough free system memory” error.</p><p>Through trial and error, I have found that system call number <code>617</code> is the last Sony system call, anything higher is unimplemented.</p><p>From this, we can conclude that there are 85 custom Sony system calls in the PS4’s kernel (617 - 532).</p><h2 id="libkernel-sprx"><a href="#libkernel-sprx" class="headerlink" title="libkernel.sprx"></a>libkernel.sprx</h2><p>To identify how custom system calls are used by libkernel, you must first remember that it is just a modification of the standard FreeBSD 9.0 libraries.</p><p>Here’s an extract of <code>_libpthread_init</code> from <a href="https://github.com/freebsd/freebsd/blob/release/9.0.0/lib/libthr/thread/thr_init.c#L297" target="_blank" rel="noopener">thr_init.c</a>:</p><pre><code>/* * Check for the special case of this process running as * or in place of init as pid = 1: */if ((_thr_pid = getpid()) == 1) {    /*     * Setup a new session for this process which is     * assumed to be running as root.     */    if (setsid() == -1)        PANIC(&quot;Can&apos;t set session ID&quot;);    if (revoke(_PATH_CONSOLE) != 0)        PANIC(&quot;Can&apos;t revoke console&quot;);    if ((fd = __sys_open(_PATH_CONSOLE, O_RDWR)) &amp;lt; 0)        PANIC(&quot;Can&apos;t open console&quot;);    if (setlogin(&quot;root&quot;) == -1)        PANIC(&quot;Can&apos;t set login to root&quot;);    if (_ioctl(fd, TIOCSCTTY, (char *) NULL) == -1)        PANIC(&quot;Can&apos;t set controlling terminal&quot;);}</code></pre><p>The same function can be found at offset <code>0x215F0</code> from <code>libkernel.sprx</code>. This is how the above extract looks from within a libkernel dump:</p><pre><code>call    getpidmov     cs:dword_5B638, eaxcmp     eax, 1jnz     short loc_2169Fcall    setsidcmp     eax, 0FFFFFFFFhjz      loc_21A0Clea     rdi, aDevConsole ; &quot;/dev/console&quot;call    revoketest    eax, eaxjnz     loc_21A24lea     rdi, aDevConsole ; &quot;/dev/console&quot;mov     esi, 2xor     al, alcall    openmov     r14d, eaxtest    r14d, r14djs      loc_21A3Clea     rdi, aRoot       ; &quot;root&quot;call    setlogincmp     eax, 0FFFFFFFFhjz      loc_21A54mov     edi, r14dmov     esi, 20007461hxor     edx, edxxor     al, alcall    ioctlcmp     eax, 0FFFFFFFFhjz      loc_21A6C</code></pre><h2 id="Reversing-module-dumps-to-analyse-system-calls"><a href="#Reversing-module-dumps-to-analyse-system-calls" class="headerlink" title="Reversing module dumps to analyse system calls"></a>Reversing module dumps to analyse system calls</h2><p>libkernel isn’t completely open source though; there’s also a lot of custom code which can help disclose some of Sony’s system calls.</p><p>Although this process will vary depending on the system call you are looking up; for some, it is fairly easy to get a basic understanding of the arguments that are passed to it.</p><p>The system call wrapper will be declared somewhere in <code>libkernel.sprx</code>, and will almost always follow this template:</p><pre><code>000000000000DB70 syscall_601     proc near000000000000DB70                 mov     rax, 259h000000000000DB77                 mov     r10, rcx000000000000DB7A                 syscall000000000000DB7C                 jb      short error000000000000DB7E                 retn000000000000DB7F000000000000DB7F error:000000000000DB7F                 lea     rcx, sub_DF60000000000000DB86                 jmp     rcx000000000000DB86 syscall_601     endp</code></pre><p>Note that the <code>mov r10, rcx</code> instruction doesn’t necessarily mean that the system call takes at least 4 arguments; all system call wrappers have it, even those that take no arguments, such as <code>getpid</code>.</p><p>Once you’ve found the wrapper, you can look up xrefs to it:</p><pre><code>0000000000011D50                 mov     edi, 10h0000000000011D55                 xor     esi, esi0000000000011D57                 mov     edx, 10000000000011D5C                 call    syscall_6010000000000011D61                 test    eax, eax0000000000011D63                 jz      short loc_11D6A</code></pre><p>It’s good to look up several of these, just to make sure that the registers weren’t modified for something unrelated:</p><pre><code>0000000000011A28                 mov     edi, 90000000000011A2D                 xor     esi, esi0000000000011A2F                 xor     edx, edx0000000000011A31                 call    syscall_6010000000000011A36                 test    eax, eax0000000000011A38                 jz      short loc_11A3F</code></pre><p>Consistently, the first three registers of the system call convention (<code>rdi</code>, <code>rsi</code>, and <code>rdx</code>) are modified before invoking the call, so we can conclude with reasonable confidence that it takes 3 arguments.</p><p>For clarity, this is how we would replicate the calls in JuSt-ROP:</p><pre><code>chain.syscall(&quot;unknown&quot;, 601, 0x10, 0, 1);chain.syscall(&quot;unknown&quot;, 601, 9, 0, 0);</code></pre><p>As with most system calls, it will return 0 on success, as seen by the <code>jz</code> conditional after <code>test</code>ing the return value.</p><p>Looking up anything beyond than the amount of arguments will require a much more in-depth analysis of the code before and after the call to understand the context, but this should help you get started.</p><h2 id="Brute-forcing-system-calls"><a href="#Brute-forcing-system-calls" class="headerlink" title="Brute forcing system calls"></a>Brute forcing system calls</h2><p>Although reverse engineering module dumps is the most reliable way to identify system calls, some aren’t referenced at all in the dumps we have so we will need to analyse them blindly.</p><p>If we guess that a certain system call might take a particular set of arguments, we can brute force all system calls which return a certain value (<code>0</code> for success) with the arguments that we chose, and ignore all which returned an error.</p><p>We can also pass 0s for all arguments, and brute force all system calls which return useful errors such as <code>0xe</code>, <code>&quot;Bad address&quot;</code>, which would indicate that they take at least one pointer.</p><p>Firstly, we will need to execute the ROP chain as soon as the page loads. We can do this by attaching our function to the <code>body</code> element’s <code>onload</code>:</p><pre><code>&lt;body onload=&quot;exploit()&quot;&gt;</code></pre><p>Next we will need to perform a specific system call depending on an HTTP GET value. Although this can be done with JavaScript, I will demonstrate how to do this using PHP for simplicity:</p><pre><code>var Sony = 533;chain.syscall(&quot;Sony system call&quot;, Sony + &amp;lt;?php print($_GET[&quot;b&quot;]); ?&amp;gt;, 0, 0, 0, 0, 0, 0);chain.write_rax_ToVariable(0);</code></pre><p>Once the system call has executed, we can check the return value, and if it isn’t interesting, redirect the page to the next system call:</p><pre><code>if(chain.getVariable(0) == 0x16) window.location.assign(&quot;index.php?b=&quot; + (&amp;lt;?php print($_GET[&quot;b&quot;]); ?&amp;gt; + 1).toString());</code></pre><p>Running the page with <code>?b=0</code> appended to the end will start the brute force from the first Sony system call.</p><p>Although this method requires a lot of experimentation, by passing different values to some of the system calls found by brute forcing and analysing the new return values, there are a few system calls which you should be able to partially identify.</p><h2 id="System-call-538"><a href="#System-call-538" class="headerlink" title="System call 538"></a>System call 538</h2><p>As an example, I’ll take a look at system call 538, without relying on any module dumps.</p><p>These are the return values depending on what is passed as the first argument:</p><ul><li>0 - <code>0x16</code>, <code>&quot;Invalid argument&quot;</code></li><li>1 - <code>0xe</code>, <code>&quot;Bad address&quot;</code></li><li>Pointer to 0s - <code>0x64</code> initially, but each time the page is refreshed this value increases by 1</li></ul><p>Other potential arguments to try would be PID, thread ID, and file descriptor.</p><p>Although most system calls will return <code>0</code> on success, due to the nature of the return value increasing after each time it is called, it seems like it is allocating a resource number, such as a file descriptor. </p><p>The next thing to do would be to look at the data before and after performing the system call, to see if it has been written to.</p><p>Since there is no change in the data, we can assume that it is an input for now.</p><p>I then tried passing a long string as the first argument. You should always try this with every input you find because there is the possibility of discovering a buffer overflow.</p><pre><code>writeString(chain.data, &quot;aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa&quot;);chain.syscall(&quot;unknown&quot;, 538, chain.data, 0, 0, 0, 0, 0);</code></pre><p>The return value for this is <code>0x3f</code>, <code>ENAMETOOLONG</code>. Unfortunately it seems that this system call correctly limits the name (32 bytes including <code>NULL</code> truncator), but it does tell us that it _is_ expecting a string, rather than a struct.</p><p>We now have a few possibilities for what this system call is doing, the most obvious being something related to the filesystem (such as a custom <code>mkdir</code> or <code>open</code>), but this doesn’t seem particularly likely seeing as a resource was allocated even before we wrote any data to the pointer.</p><p>To test whether the first parameter is a path, we can break it up with multiple <code>/</code> characters to see if this allows for a longer string:</p><pre><code>writeString(chain.data, &quot;aaaaaaaaaa/aaaaaaaaaa/aaaaaaaaaa&quot;);chain.syscall(&quot;unknown&quot;, 538, chain.data, 0, 0, 0, 0, 0);</code></pre><p>Since this also returns <code>0x3f</code>, we can assume that the first argument isn’t a path; it is a name for <em>something</em> that gets allocated a sequential identifier.</p><p>After analysing some more system calls, I found that the following all shared this exact same behaviour:</p><ul><li>533</li><li>538</li><li>557</li><li>574</li><li>580</li></ul><p>From the information that we have so far, it is almost impossible to pinpoint exactly what these system calls do, but as you run more tests, further information will slowly be revealed.</p><p>To save you some time, system call 538 is allocating an event flag (and it doesn’t just take a name).</p><p>Using general knowledge of how a kernel works, you can guess, and then verify, what the system calls are allocating (semaphores, mutexes, etc).</p><h2 id="Dumping-additional-modules"><a href="#Dumping-additional-modules" class="headerlink" title="Dumping additional modules"></a>Dumping additional modules</h2><p>We can dump additional modules by following these stages:</p><ul><li>Load the module</li><li>Get the module’s base address</li><li>Dump the module</li></ul><p>I’ve extracted and posted a list of all module names on <a href="http://www.psdevwiki.com/ps4/Libraries#Libraries_on_firmware_1.76" target="_blank" rel="noopener">psdevwiki</a>.</p><p>To load a module we will need to use the <code>sceSysmoduleLoadModule</code> function from <code>libSceSysmodule.sprx + 0x1850</code>. The first parameter is the module ID to load, and the other 3 should just be passed 0.</p><p>The following JuSt-ROP method can be used to perform a function call:</p><pre><code>this.call = function(name, module, address, arg1, arg2, arg3, arg4, arg5, arg6) {    console.log(&quot;call &quot; + name);    if(typeof(arg1) !== &quot;undefined&quot;) this.add(&quot;pop rdi&quot;, arg1);    if(typeof(arg2) !== &quot;undefined&quot;) this.add(&quot;pop rsi&quot;, arg2);    if(typeof(arg3) !== &quot;undefined&quot;) this.add(&quot;pop rdx&quot;, arg3);    if(typeof(arg4) !== &quot;undefined&quot;) this.add(&quot;pop rcx&quot;, arg4);    if(typeof(arg5) !== &quot;undefined&quot;) this.add(&quot;pop r8&quot;, arg5);    if(typeof(arg6) !== &quot;undefined&quot;) this.add(&quot;pop r9&quot;, arg6);    this.add(module_bases[module] + address);}</code></pre><p>So, to load <code>libSceAvSetting.sprx</code> (<code>0xb</code>):</p><pre><code>chain.call(&quot;sceSysmoduleLoadModule&quot;, libSysmodule, 0x1850, 0xb, 0, 0, 0);</code></pre><p>Unfortunately, a fault will be triggered when trying to load certain modules; this is because the <code>sceSysmoduleLoadModule</code> function doesn’t load dependencies, so you will need to manually load them first.</p><p>Like most system calls, this should return <code>0</code> on success. To see the loaded module ID that was allocated, we can use one of Sony’s custom system calls, number <code>592</code>, to get a list of currently loaded modules:</p><pre><code>var countAddress = chain.data;var modulesAddress = chain.data + 8;// System call 592, getLoadedModules(int *destinationModuleHandles, int max, int *count);chain.syscall(&quot;getLoadedModules&quot;, 592, modulesAddress, 256, countAddress);chain.execute(function() {    var count = getU64from(countAddress);    for(var index = 0; index &amp;lt; count; index++) {        logAdd(&quot;Module handle: 0x&quot; + getU32from(modulesAddress + index * 4).toString(16));    }});</code></pre><p>Running this without loading any additional modules will produce the following list:</p><pre><code>0x0, 0x1, 0x2, 0xc, 0xe, 0xf, 0x11, 0x12, 0x13, 0x14, 0x15, 0x16, 0x17, 0x18, 0x19, 0x1a, 0x1b, 0x1e, 0x37, 0x59</code></pre><p>But if we run it after loading module <code>0xb</code>, we will see an additional entry, <code>0x65</code>. Remember that module ID is <em>not</em> the same as loaded module handle.</p><p>We can now use another of Sony’s custom system calls, number <code>593</code>, which takes a module handle and a buffer, and fills the buffer with information about the loaded module, including its base address. Since the next available handle is always <code>0x65</code>, we can hardcode this value into our chain, rather than having to store the result from the module list.</p><p>The buffer must start with the size of the struct that should be returned, otherwise error <code>0x16</code> will be returned, <code>&quot;Invalid argument&quot;</code>:</p><pre><code>setU64to(moduleInfoAddress, 0x160);chain.syscall(&quot;getModuleInfo&quot;, 593, 0x65, moduleInfoAddress);chain.execute(function() {    logAdd(hexDump(moduleInfoAddress, 0x160));});</code></pre><p>It will return <code>0</code> upon success, and fill the buffer with a struct which can be read like so:</p><pre><code>var name = readString(moduleInfoAddress + 0x8);var codeBase = getU64from(moduleInfoAddress + 0x108);var codeSize = getU32from(moduleInfoAddress + 0x110);var dataBase = getU64from(moduleInfoAddress + 0x118);var dataSize = getU32from(moduleInfoAddress + 0x120);</code></pre><p>We now have everything we need to dump the module!</p><pre><code>dump(codeBase, codeSize + dataSize);</code></pre><p>There is another Sony system call, number <code>608</code>, which works in a similar way to <code>593</code>, but provides slightly different information about the loaded module:</p><pre><code>setU64to(moduleInfoAddress, 0x1a8);chain.syscall(&quot;getDifferentModuleInfo&quot;, 608, 0x65, 0, moduleInfoAddress);logAdd(hexDump(moduleInfoAddress, 0x1a8));</code></pre><p>It’s not clear what this information is.</p><h2 id="Browsing-the-filesystem"><a href="#Browsing-the-filesystem" class="headerlink" title="Browsing the filesystem"></a>Browsing the filesystem</h2><p>The PS4 uses the standard FreeBSD 9.0 system calls for reading files and directories.</p><p>However, whilst using <code>read</code> for some directories such as <code>/dev/</code> will work, others, such as <code>/</code> will fail.</p><p>I’m not sure why this is, but if we use <code>getdents</code> instead of <code>read</code> for directories, it will work much more reliably:</p><pre><code>writeString(chain.data, &quot;/dev/&quot;);chain.syscall(&quot;open&quot;, 5, chain.data, 0, 0);chain.write_rax_ToVariable(0);chain.read_rdi_FromVariable(0);chain.syscall(&quot;getdents&quot;, 272, undefined, chain.data + 0x10, 1028);</code></pre><p>This is the resultant memory:</p><pre><code>0000010: 0700 0000 1000 0205 6469 7073 7700 0000  ........dipsw...0000020: 0800 0000 1000 0204 6e75 6c6c 0000 0000  ........null....0000030: 0900 0000 1000 0204 7a65 726f 0000 0000  ........zero....0000040: 0301 0000 0c00 0402 6664 0000 0b00 0000  ........fd......0000050: 1000 0a05 7374 6469 6e00 0000 0d00 0000  ....stdin.......0000060: 1000 0a06 7374 646f 7574 0000 0f00 0000  ....stdout......0000070: 1000 0a06 7374 6465 7272 0000 1000 0000  ....stderr......0000080: 1000 0205 646d 656d 3000 0000 1100 0000  ....dmem0.......0000090: 1000 0205 646d 656d 3100 0000 1300 0000  ....dmem1.......00000a0: 1000 0206 7261 6e64 6f6d 0000 1400 0000  ....random......00000b0: 1000 0a07 7572 616e 646f 6d00 1600 0000  ....urandom.....00000c0: 1400 020b 6465 6369 5f73 7464 6f75 7400  ....deci_stdout.00000d0: 1700 0000 1400 020b 6465 6369 5f73 7464  ........deci_std00000e0: 6572 7200 1800 0000 1400 0209 6465 6369  err.........deci00000f0: 5f74 7479 3200 0000 1900 0000 1400 0209  _tty2...........0000100: 6465 6369 5f74 7479 3300 0000 1a00 0000  deci_tty3.......0000110: 1400 0209 6465 6369 5f74 7479 3400 0000  ....deci_tty4...0000120: 1b00 0000 1400 0209 6465 6369 5f74 7479  ........deci_tty0000130: 3500 0000 1c00 0000 1400 0209 6465 6369  5...........deci0000140: 5f74 7479 3600 0000 1d00 0000 1400 0209  _tty6...........0000150: 6465 6369 5f74 7479 3700 0000 1e00 0000  deci_tty7.......0000160: 1400 020a 6465 6369 5f74 7479 6130 0000  ....deci_ttya0..0000170: 1f00 0000 1400 020a 6465 6369 5f74 7479  ........deci_tty0000180: 6230 0000 2000 0000 1400 020a 6465 6369  b0.. .......deci0000190: 5f74 7479 6330 0000 2200 0000 1400 020a  _ttyc0..&quot;.......00001a0: 6465 6369 5f73 7464 696e 0000 2300 0000  deci_stdin..#...00001b0: 0c00 0203 6270 6600 2400 0000 1000 0a04  ....bpf.$.......00001c0: 6270 6630 0000 0000 2900 0000 0c00 0203  bpf0....).......00001d0: 6869 6400 2c00 0000 1400 0208 7363 655f  hid.,.......sce_00001e0: 7a6c 6962 0000 0000 2e00 0000 1000 0204  zlib............00001f0: 6374 7479 0000 0000 3400 0000 0c00 0202  ctty....4.......0000200: 6763 0000 3900 0000 0c00 0203 6463 6500  gc..9.......dce.0000210: 3a00 0000 1000 0205 6462 6767 6300 0000  :.......dbggc...0000220: 3e00 0000 0c00 0203 616a 6d00 4100 0000  &gt;.......ajm.A...0000230: 0c00 0203 7576 6400 4200 0000 0c00 0203  ....uvd.B.......0000240: 7663 6500 4500 0000 1800 020d 6e6f 7469  vce.E.......noti0000250: 6669 6361 7469 6f6e 3000 0000 4600 0000  fication0...F...0000260: 1800 020d 6e6f 7469 6669 6361 7469 6f6e  ....notification0000270: 3100 0000 5000 0000 1000 0206 7573 6263  1...P.......usbc0000280: 746c 0000 5600 0000 1000 0206 6361 6d65  tl..V.......came0000290: 7261 0000 8500 0000 0c00 0203 726e 6700  ra..........rng.00002a0: 0701 0000 0c00 0403 7573 6200 c900 0000  ........usb.....00002b0: 1000 0a07 7567 656e 302e 3400 0000 0000  ....ugen0.4.....00002c0: 0000 0000 0000 0000 0000 0000 0000 0000  ................</code></pre><p>You can read some of these devices, for example: reading <code>/dev/urandom</code> will fill the memory with random data.</p><p>It is also possible to parse this memory to create a clean list of entries; look at <code>browser.html</code> in the repository for a complete file browser:</p><p><img src="/img/hacking-ps4-1/browser.png" alt></p><p>Unfortunately, due to sandboxing we don’t have complete access to the file system. Trying to read files and directories that <a href="http://www.psdevwiki.com/ps4/Talk:Files_on_the_PS4" target="_blank" rel="noopener">do exist</a> but are restricted will give you error 2, <code>ENOENT</code>, <code>&quot;No such file or directory&quot;</code>.</p><p>We do have access to a lot of interesting stuff though including encrypted save data, trophies, and account information. I will go over more of the filesystem in my next article.</p><h2 id="Sandboxing"><a href="#Sandboxing" class="headerlink" title="Sandboxing"></a>Sandboxing</h2><p>As well as file related system calls failing for certain paths, there are other reasons for a system call to fail.</p><p>Most commonly, a disallowed system call will just return error <code>1</code>, <code>EPERM</code>, <code>&quot;Operation not permitted&quot;</code>; such as trying to use <code>ptrace</code>, but other system calls may fail for different reasons:</p><p>Compatibilty system calls are disabled. If you are trying to call <code>mmap</code> for example, you must use <a href="http://fxr.watson.org/fxr/source/kern/syscalls.master?v=FREEBSD9#L851" target="_blank" rel="noopener">system call number 477</a>, not <a href="http://fxr.watson.org/fxr/source/kern/syscalls.master?v=FREEBSD9#L172" target="_blank" rel="noopener">71</a> or <a href="http://fxr.watson.org/fxr/source/kern/syscalls.master?v=FREEBSD9#L379" target="_blank" rel="noopener">197</a>; otherwise a segfault will be triggered.</p><p>Other system calls such as <code>exit</code> will also trigger a fault:</p><pre><code>chain.syscall(&quot;exit&quot;, 1, 0);</code></pre><p>Trying to create an SCTP socket will return error <code>0x2b</code>, <code>EPROTONOSUPPORT</code>, indicating that SCTP sockets have been disabled in the PS4 kernel:</p><pre><code>//int socket(int domain, int type, int protocol);//socket(AF_INET, SOCK_STREAM, IPPROTO_SCTP);chain.syscall(&quot;socket&quot;, 97, 2, 1, 132);</code></pre><p>And although calling <code>mmap</code> with <code>PROT_READ | PROT_WRITE | PROT_EXEC</code> will return a valid pointer, the <code>PROT_EXEC</code> flag is ignored. Reading its protection will return 3 (RW), and any attempt to execute the memory will trigger a segfault:</p><pre><code>chain.syscall(&quot;mmap&quot;, 477, 0, 4096, 1 | 2 | 4, 4096, -1, 0);chain.write_rax_ToVariable(0);chain.read_rdi_FromVariable(0);chain.add(&quot;pop rax&quot;, 0xfeeb);chain.add(&quot;mov [rdi], rax&quot;);chain.add(&quot;mov rax, rdi&quot;);chain.add(&quot;jmp rax&quot;);</code></pre><p>The list of <a href="http://www.scei.co.jp/ps4-license/" target="_blank" rel="noopener">open source software used in the PS4</a> doesn’t list any kind of sandboxing software like <a href="https://www.freebsd.org/cgi/man.cgi?query=capsicum" target="_blank" rel="noopener">Capsicum</a>, so the PS4 must use either pure <a href="https://www.freebsd.org/doc/handbook/jails.html" target="_blank" rel="noopener">FreeBSD jails</a>, or some kind of custom, proprietary, sandboxing system (unlikely).</p><h2 id="Jails"><a href="#Jails" class="headerlink" title="Jails"></a>Jails</h2><p>We can prove the existence of FreeBSD jails being actively used in the PS4’s kernel through the <code>auditon</code> system call being impossible to execute within a jailed environment:</p><pre><code>chain.syscall(&quot;auditon&quot;, 446, 0, 0, 0);</code></pre><p>The first thing the <code>auditon</code> system call does is check <code>jailed</code> <a href="https://github.com/freebsd/freebsd/blob/release/9.0.0/sys/security/audit/audit_syscalls.c#L164" target="_blank" rel="noopener">here</a>, and if so, return <code>ENOSYS</code>:</p><pre><code>if (jailed(td-&gt;td_ucred))    return (ENOSYS);</code></pre><p>Otherwise the system call would most likely return <code>EPERM</code> from the <code>mac_system_check_auditon</code> <a href="https://github.com/freebsd/freebsd/blob/release/9.0.0/sys/security/audit/audit_syscalls.c#L169" target="_blank" rel="noopener">here</a>:</p><pre><code>error = mac_system_check_auditon(td-&gt;td_ucred, uap-&gt;cmd);if (error)    return (error);</code></pre><p>Or from the <code>priv_check</code> <a href="https://github.com/freebsd/freebsd/blob/release/9.0.0/sys/security/audit/audit_syscalls.c#L174" target="_blank" rel="noopener">here</a>:</p><pre><code>error = priv_check(td, PRIV_AUDIT_CONTROL);if (error)    return (error);</code></pre><p>The absolute furthest that the system call could reach would be immediately after the <code>priv_check</code>, <a href="https://github.com/freebsd/freebsd/blob/release/9.0.0/sys/security/audit/audit_syscalls.c#L178" target="_blank" rel="noopener">here</a>, before returning <code>EINVAL</code> due to the length argument being 0:</p><pre><code>if ((uap-&gt;length &lt;= 0) || (uap-&gt;length &gt; sizeof(union auditon_udata)))    return (EINVAL);</code></pre><p>Since <code>mac_system_check_auditon</code> and <code>priv_check</code> will never return <code>ENOSYS</code>, having the <code>jailed</code> check pass is the only way <code>ENOSYS</code> could be returned.</p><p>When executing the chain, <code>ENOSYS</code> _is_ returned (<code>0x48</code>).</p><p>This tells us that whatever sandbox system the PS4 uses is at least based on jails because the <code>jailed</code> check passes.</p><h2 id="FreeBSD-9-0-kernel-exploits"><a href="#FreeBSD-9-0-kernel-exploits" class="headerlink" title="FreeBSD 9.0 kernel exploits"></a>FreeBSD 9.0 kernel exploits</h2><p>Before trying to look for new vulnerabilities in the <a href="https://github.com/freebsd/freebsd/tree/release/9.0.0/sys/kern" target="_blank" rel="noopener">FreeBSD 9.0 kernel source code</a>, we should first check whether any of the <a href="https://www.exploit-db.com/platform/?p=freebsd" target="_blank" rel="noopener">kernel vulnerabilities already found</a> could be used on the PS4.</p><p>We can immediately dismiss some of these for obvious reasons:</p><ul><li><a href="https://www.exploit-db.com/exploits/26368/" target="_blank" rel="noopener">FreeBSD 9.0-9.1 mmap/ptrace - Privilege Escalation Exploit</a> - this won’t work since, as previously stated, we don’t have access to the <code>ptrace</code> system call.</li><li><a href="https://www.exploit-db.com/exploits/28718/" target="_blank" rel="noopener">FreeBSD 9.0 - Intel SYSRET Kernel Privilege Escalation Exploit</a> - won’t work because the PS4 uses an AMD processor.</li><li><a href="https://www.exploit-db.com/exploits/35938/" target="_blank" rel="noopener">FreeBSD Kernel - Multiple Vulnerabilities</a> - maybe the first vulnerability will lead to something, but the other 2 rely on SCTP sockets, which the PS4 kernel has disabled (as previously stated).</li></ul><p>However, there are <a href="http://www.cvedetails.com/vulnerability-list/vendor_id-6/product_id-7/version_id-118765/Freebsd-Freebsd-9.0.html" target="_blank" rel="noopener">some smaller vulnerabilites</a>, which could lead to something:</p><h2 id="getlogin"><a href="#getlogin" class="headerlink" title="getlogin"></a>getlogin</h2><p>One vulnerability which looks easy to try is using the <a href="http://www.cvedetails.com/cve/CVE-2014-8476/" target="_blank" rel="noopener">getlogin system call to leak a small amount of kernel memory</a>.</p><p>The <a href="https://www.freebsd.org/cgi/man.cgi?query=getlogin&amp;sektion=2" target="_blank" rel="noopener"><code>getlogin</code></a> system call is intended to copy the login name of the current session to userland memory, however, due to a bug, the whole buffer is always copied, and not just the size of the name string. This means that we can read some uninitialised data from the kernel, which might be of some use.</p><p>Note that the system call (49) is actually <code>int getlogin_r(char *name, int len);</code> and not <code>char *getlogin(void);</code>.</p><p>So, let’s try copying some kernel memory into an unused part of userland memory:</p><pre><code>chain.syscall(&quot;getlogin&quot;, 49, chain.data, 17);</code></pre><p>Unfortunately 17 bytes is the most data we can get, since:</p><blockquote><p>Login names are limited to <code>MAXLOGNAME</code> (from <code>&amp;lt;sys/param.h&amp;gt;</code>) characters, currently 17 including null.</p><p>– <cite><a href="https://www.freebsd.org/cgi/man.cgi?query=getlogin&amp;sektion=2" target="_blank" rel="noopener">FreeBSD Man Pages</a></cite></p></blockquote><p>After executing the chain, the return value was 0, which means that the system call worked! An excellent start. Now let’s take a look at the memory which we pointed to:</p><p>Before executing the chain:</p><pre><code>00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 0000</code></pre><p>After executing the chain:</p><pre><code>72 6f 6f 74 00 fe ff ff 08 62 61 82 ff ff ff ff00</code></pre><p>After decoding the first 4 bytes as ASCII:</p><pre><code>root</code></pre><p>So the browser is executed as root! That was unexpected.</p><p>But more interestingly, the memory leaked looks like a pointer to something in the kernel, which is always the same each time the chain is run; this is evidence to support <a href="https://twitter.com/yifanlu/status/551498289284009984" target="_blank" rel="noopener">Yifanlu’s claims that the PS4 has no Kernel ASLR</a>!</p><h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>From the information currently available, the PS4’s kernel seems to be very similar to the stock FreeBSD 9.0 kernel.</p><p>Importantly, the differences that <em>are</em> present appear to be from standard <a href="https://www.freebsd.org/doc/handbook/kernelconfig-custom-kernel.html" target="_blank" rel="noopener">kernel configuration</a> changes (such as disabling SCTP sockets), rather than from modified code. Sony have also added several of their own custom system calls to the kernel, but apart from this, the rest of the kernel seems fairly untouched.</p><p>In this respect, I’m inclined to believe that the PS4 shares most of the same juicy vulnerabilities as FreeBSD 9.0’s kernel!</p><p>Unfortunately, most kernel exploits cannot be triggered from the WebKit entry point that we currently have due to sandboxing constraints (likely to be just stock FreeBSD jails).</p><p>And with FreeBSD 10 being out, it’s unlikely that anyone is stashing away any private exploits for FreeBSD 9, so unless a new one is suddenly released, we’re stuck with what is currently available.</p><p>The best approach from here seems to be reverse engineering all of the modules which can be dumped, in order to document as many of Sony’s custom system calls as possible; I have a hunch that we will have more luck targeting these, than the standard FreeBSD system calls.</p><p>Recently <a href="http://hackinformer.com/2015/06/24/breaking-news-discovered-two-communication-ports-uart-playstation-4/" target="_blank" rel="noopener">Jaicrab has discovered two UART ports on the PS4</a> which shows us that there are hardware hackers interested in the PS4. Although the role of hardware hackers has traditionally been to dump the RAM of a system, <a href="http://farm4.static.flickr.com/3441/3869187499_da1665050d.jpg" target="_blank" rel="noopener">like with the DSi</a>, which we can already do thanks to the WebKit exploit, there’s also the possibility of a hardware triggered kernel vulnerability being found, like <a href="http://rdist.root.org/2010/01/27/how-the-ps3-hypervisor-was-hacked/" target="_blank" rel="noopener">geohot’s original PS3 hypervisor hack</a>. It remains most likely that a kernel exploit will be found on the PS4 through system call vulnerabilities though.</p><p><a href="http://cturt.github.io/ps4.html" target="_blank" rel="noopener">原文地址</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;em&gt;原文标题 Hacking the PS4, part 1 Introduction to PS4’s security, and userland ROP (日期未知)&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;作者：&lt;a href=&quot;https://github.com/CTurt&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;CTurt&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;由于目前很长时间没有关于 PS4 破解的重大公告，我想解释一下 PS4 破解已经走了多远，以及阻止它进一步发展的原因。&lt;/p&gt;
&lt;p&gt;我将解释一些通用于所有现代系统的安全概念，以及我在 PS4 上运行 ROP 测试所做的发现。&lt;/p&gt;
&lt;p&gt;本系列文章的目标是展示完整的漏洞利用链，从访问 Internet 浏览器的网页，到最终在 PS4 上实现内核代码执行。&lt;/p&gt;
&lt;p&gt;如果您对漏洞挖掘不是特别熟悉，那么您应该先阅读我的关于&lt;a href=&quot;http://cturt.github.io/DS-exploit-finding.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;通过保存文件中的栈粉碎漏洞来破解 DS 游戏&lt;/a&gt;的文章。&lt;/p&gt;
&lt;p&gt;您可以在此下载我的&lt;a href=&quot;https://github.com/CTurt/PS4-playground&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;完整设置&lt;/a&gt;以自行运行这些测试；它目前仅适用于固件 1.76。如果您正在使用较旧的固件并希望更新到 1.76，则可以下载 &lt;a href=&quot;http://www.psdevwiki.com/ps4/1.760.000&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;1.76 PUP&lt;/a&gt; 文件并&lt;a href=&quot;https://www.playstation.com/en-us/support/system-updates/ps4/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;通过 USB 更新&lt;/a&gt;。&lt;/p&gt;
    
    </summary>
    
      <category term="硬件分析" scheme="http://zanderchang.github.io/categories/%E7%A1%AC%E4%BB%B6%E5%88%86%E6%9E%90/"/>
    
    
      <category term="外文翻译" scheme="http://zanderchang.github.io/tags/%E5%A4%96%E6%96%87%E7%BF%BB%E8%AF%91/"/>
    
      <category term="漏洞挖掘" scheme="http://zanderchang.github.io/tags/%E6%BC%8F%E6%B4%9E%E6%8C%96%E6%8E%98/"/>
    
      <category term="PS4" scheme="http://zanderchang.github.io/tags/PS4/"/>
    
      <category term="硬件分析" scheme="http://zanderchang.github.io/tags/%E7%A1%AC%E4%BB%B6%E5%88%86%E6%9E%90/"/>
    
  </entry>
  
  <entry>
    <title>VBA Stomping 简介</title>
    <link href="http://zanderchang.github.io/2019/04/30/VBA-Stomping%E7%AE%80%E4%BB%8B/"/>
    <id>http://zanderchang.github.io/2019/04/30/VBA-Stomping简介/</id>
    <published>2019-04-30T02:17:09.000Z</published>
    <updated>2019-05-06T02:57:16.433Z</updated>
    
    <content type="html"><![CDATA[<p><em>原文标题 VBA Stomping — Advanced Maldoc Techniques (Oct 6, 2018)</em></p><p>作者：Kirk Sayre <a href="https://twitter.com/bigmacjpg" target="_blank" rel="noopener">@bigmacjpg</a>，Harold Ogden <a href="https://twitter.com/haroldogden" target="_blank" rel="noopener">@haroldogden</a> 和 Carrie Roberts <a href="https://twitter.com/OrOneEqualsOne" target="_blank" rel="noopener">@OrOneEqualsOne</a></p><p>VBA Stomping 是一种可以绕过反病毒检测恶意文档生成技术，它最初由 Vesselin Bontchev 博士引起我们的注意（见<a href="https://github.com/bontchev/pcodedmp" target="_blank" rel="noopener">此处</a>）。VBA stomping 是指销毁 Microsoft Office 文档中的 VBA 源代码，只留下文档文件中称为 p-code 的宏代码的编译版本。在这种情况下，仅基于VBA源代码的恶意文档检测会失败。在这篇博文中，我们将详细演示 VBA stomping 并介绍一些其他技术。</p><a id="more"></a><h2 id="VBA-Stomping"><a href="#VBA-Stomping" class="headerlink" title="VBA Stomping"></a>VBA Stomping</h2><p>首先，我们将在一个简单的非恶意宏上演示 VBA Stomping。此文档在打开时显示带有文本 ABC 的消息框。VBA 源代码和生成的消息框如下所示。</p><p><img src="/img/vba-stomping/1_P_mM0Ffo9kzzhtMM1O7oCg.png" alt></p><p>现在我们修改上面显示的VBA源代码，同时保持中间 p-code 不变。我们将集中讨论当前的 .docx/.xlsx/.docm/.xlsm（Office 2007 以上）格式。但是，此处讨论的技术可以轻松应用于较旧的 .doc/.xls格式。在 Office 2007 以上文件中，VBA 源代码和 p-code 通常位于名为 vbaProject.bin 的文件中。请注意这是默认文件名，可以被重命名。如果要手动修改此文件，我们需要首先解压 .docm/.xlsm 文件，然后在十六进制编辑器中打开 vbaProject.bin 文件。在此示例中，我们将 ABC 改为 XYZ，但仅限于存储 VBA 源代码的位置，而不是 p-code 部分。VBA 源代码以压缩形式存储，即下图中不可打印或奇怪的字符。</p><p><img src="/img/vba-stomping/1_KGc1XphbyNT5Dws5W8lOPQ.png" alt></p><p><img src="/img/vba-stomping/1_iHWUB-LOeGuw_8K02Gixpg.png" alt></p><p>现在我们已经手动编辑了 VBA 源代码将 ABC 更改为 XYZ，我们打开文档并在单击“启用内容”按钮之前检查 VBA 源代码。</p><p><img src="/img/vba-stomping/1_jqYto7TMBR3MUZPqzdhogw.png" alt></p><p>我们打开文档但未启用宏。在代码编辑器中检查宏，代码显示宏将显示带有文本 XYZ 的消息框，但事实并非如此。实际上，只要启用了内容，就会显示一个 ABC 的消息框，并且代码编辑器中的源代码会更新以匹配！</p><p><img src="/img/vba-stomping/1_VE19x2i45U7r6FXDxov1UA.png" alt></p><p>源代码表示 XYZ 将显示在消息框中，但显示 ABC 。到底是怎么回事？</p><p>正如 Bontchev 博士所解释的那样，只要 p-code 与系统上的当前 VBA 版本兼容，文档实际执行的是存储的 p-code。此外，宏编辑器中显示的内容（一旦启用内容）并不是解压的 VBA 源代码，而是反编译的 p-code。</p><p>如果我们在不同版本的 Word（使用不同的 VBA 版本）中打开文档，则 p-code 将不可重用。这将强制将 VBA 源代码解压缩并重新编译为 p-code，从而在消息框中显示 XYZ。所以现在我们有一个文档，在一个版本的Office上显示 ABC，但在另一个版本上显示 XYZ。</p><p>请注意，这很重要。<strong>使用 VBA Stomping 技术的恶意文档只能使用用于创建文档时相同的 VBA 版本执行。</strong> 我们可以通过在恶意文档生成之前对目标进行侦察来确定要使用的适当 Office 版本适当 Office 版本；或者通过生成具有多个 Office 版本的恶意文档并将其喷射到目标上来解决此限制。</p><p>从防守角度看 VBA stomping 是什么效果呢？通常恶意文档检测仅基于 VBA 源代码。甚至许多可用于分析文档的工具都无法识别 VBA 源和 p-code 之间的差异。由 Philippe Lagadec 编写的 <a href="https://github.com/decalage2/oletools/wiki/olevba" target="_blank" rel="noopener">olevba 脚本</a>对篡改文档的分析仅显示了解压缩的源代码，且缺少 p-code 细节。</p><p><img src="/img/vba-stomping/1_TIq1-TyPn5gxopcr88-U9A.png" alt></p><p>除非使用 p-code dumper 插件运行，否则 Didier Stevens 的 Python 脚本 <a href="https://blog.didierstevens.com/programs/oledump-py/" target="_blank" rel="noopener">oledump.py</a> 也会给出类似的结果。在使用该插件的情况下，输出给出了一些指示，即 p-code 将使消息框显示 ABC，如下图所示。</p><p><img src="/img/vba-stomping/1_ZmBHbwFxuqbZB9JZzN4yrw.png" alt></p><p>Bontchev 博士发布了一个名为 <a href="https://github.com/bontchev/pcodedmp" target="_blank" rel="noopener">pcodedmp.py</a> 的 Python 脚本用于显示 p-code，如下图所示。输出可以显示操作名称而不是上面的操作码。</p><p><img src="/img/vba-stomping/1_1jU7iSKRyTb93EK5WfIZ3Q.png" alt></p><p>我们可以通过用零或随机字节覆盖 VBA 源代码来完全擦除（stomp），而不仅仅是修改它。以下截图显示了如何在十六进制编辑器中手动执行此操作。</p><p><img src="/img/vba-stomping/1_xCsSmTV9k0qFMjUWfQDWuQ.png" alt></p><p><img src="/img/vba-stomping/1_XLiGHjipu6lk7JU8rNxzXw.png" alt></p><p>在这种情况下，VBA 编辑器根本不显示宏代码，但启用内容后仍然显示带有文本 ABC 的消息框。现在，我们在此文件上重新运行 olevba，结果不显示任何VBA源代码，并显示“未找到可疑关键字或 IOC”，而不是之前突出显示我们在使用可疑关键字（AutoOpen）。</p><p><img src="/img/vba-stomping/1_3t0jKe01yE0ydhE2pFXvxA.png" alt></p><h1 id="这个问题有多严重"><a href="#这个问题有多严重" class="headerlink" title="这个问题有多严重?"></a>这个问题有多严重?</h1><p>我们现在将更深入地了解 Office 如何使用压缩的 VBA 源代码和 p-code。稍后当我们讨论如何滥用 Office 功能来轻松修改恶意文档以欺骗许多 AV 扫描时，这将会发挥作用。</p><p>Office 文档包含两个用于提取 VBA 的位置，即压缩的 VBA 源代码和 p-code。下表详细说明了在以下情况下使用哪个 VBA 数据源：</p><p><img src="/img/vba-stomping/1_Eh5SA794dEMFOlkqR8ThDQ.png" alt></p><p>从该表中我们可以看到，如果我们有有效的 p-code，则在启用宏时它会忽略压缩的 VBA 源代码，并通过运行 p-code 来执行所有宏功能。作为攻击者，这告诉我们，我们可以完全破坏或修改压缩的 VBA 源代码，并且仍然让我们的恶意文档执行其指定的任务。</p><h1 id="现实案例"><a href="#现实案例" class="headerlink" title="现实案例"></a>现实案例</h1><p>上述表明这适用于简单示例，但这真的是威胁吗？让我们通过使用已知的恶意文档将其提升到新的水平。在这种情况下，我们将从最近的 <a href="https://www.cyber.nj.gov/threat-profiles/trojan-variants/emotet" target="_blank" rel="noopener">Emotet</a> 恶意文档开始，它目前在 Virus total 上被 36/59 的供应商检测为恶意。</p><p><img src="/img/vba-stomping/1_vhePAlQl6xO7TLdXXW1mCg.png" alt></p><p>如果我们使用这个文档并且如上所述方法简单地处理 VBA 源代码，那么检测率将降至 58 种防病毒解决方案中的 7 种！</p><p><img src="/img/vba-stomping/1_15XLHrfFRP4PcFoTSX1GCA.png" alt></p><p>如您所见，这给网络防御者带来了严重的问题。即使手动分析此类文档也可能存在问题。此时显而易见的问题是<em>可以通过工具自动执行 VBA stomping 还是我们必须手动编辑 Office 文档？</em> 这个问题的答案是肯定的。我们开发了一个 POC 工具来自动在任何 Office 文档中处理压缩的 VBA 源代码（它被用于 VBA stomping 示例的 Emotet 文档，我们不会发布此实用程序）。考虑到自动化 VBA stomping 十分容易，这是一个真正的威胁。</p><h1 id="变得更糟"><a href="#变得更糟" class="headerlink" title="变得更糟"></a>变得更糟</h1><p>考虑恶意文档被设计为在执行恶意有效载荷之后立即关闭 MS Word 的情况。与 VBA stomping 结合使用时，没有脱机的 VBA 源代码提取工具将显示 VBA 源代码，并且在启用宏之前，VBA 源代码甚至不会显示在 Office 宏编辑器窗口中。但是启用宏会导致 Word 立即退出而不提供查看反编译源的机会。虽然我们已经证明“类似汇编”的 p-code 是可提取的，但是 p-code 很难解释，并且无法在 VBA 调试器中被分析。此外 p-code 仅在特定的VBA版本上运行。出于这些原因，访问 VBA 源代码对分析人员来说是一个很大的好处。但是如何才能为这样的文件做到这一点呢？</p><p>经过一些实验，我们找到了一个简单的解决方案，可以在宏启用后立即停止 MS Word 执行任何方法。请注意，此解决方案适用于 Office 2007 以上的文档，我们研究解决较旧的文档格式。Office 2007 以上的解决方案是从 .docm 文件的 word 目录中删除 vbaData.xml 文件，如下所示（这可以通过 7-Zip 程序完成，而无需手动解压缩和重新压缩文档）。</p><p><img src="/img/vba-stomping/1_j953lLi4vkji5nySs3YPeg.png" alt></p><p>现在我们可以打开文档并启用内容，从而在 VBA 代码编辑器中显示反编译的 p-code，但不执行代码。如果恶意软件作者已采取措施阻止分析人员访问 VBA 源代码，则这种方法可以节省时间。</p><p>删除 vbaData.xml 的一个有趣的副作用是它会导致 MS Word 在宏对话框中错误地列出没有宏（参见下图）。</p><p><img src="/img/vba-stomping/1_KIhYa2asfDGRAIWXERhXug.png" alt></p><h1 id="检测"><a href="#检测" class="headerlink" title="检测"></a>检测</h1><p>我们编写了一个开源工具，用于检测名为 VBA Seismograph 的 Office 文档中的 VBA stomping。此工具已在 Ubuntu 16.04 下测试，它检测声明的函数/变量名称、字符串文字以及出现在已编译的 p-code 和 Office 文档的 VBA 源代码中的注释行之间的差异。该工具可在<a href="https://github.com/kirk-sayre-work/VBASeismograph" target="_blank" rel="noopener">这里</a>找到。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>在这篇博客文章中，我们演示了如何修改或销毁 Office 文档中压缩的 VBA 源代码，来绕过 AV 扫描，并使恶意文档的手动分析更加困难。对此技术的防御涉及检测和标记具有有效 p-code 但无效或缺少 VBA 源代码的文档，或更一般地，检查压缩 VBA 源代码与反编译 p-code 之间的差异。目前，我们还没有发现任何进行此项检查的商用 AV 解决方案，因此这是 AV 扫描解决方案可以改进的领域。</p><h1 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h1><ul><li><a href="https://vbastomp.com/" target="_blank" rel="noopener">https://vbastomp.com/</a></li></ul><p><a href="https://medium.com/walmartlabs/vba-stomping-advanced-maldoc-techniques-612c484ab278" target="_blank" rel="noopener">原文地址</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;em&gt;原文标题 VBA Stomping — Advanced Maldoc Techniques (Oct 6, 2018)&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;作者：Kirk Sayre &lt;a href=&quot;https://twitter.com/bigmacjpg&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;@bigmacjpg&lt;/a&gt;，Harold Ogden &lt;a href=&quot;https://twitter.com/haroldogden&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;@haroldogden&lt;/a&gt; 和 Carrie Roberts &lt;a href=&quot;https://twitter.com/OrOneEqualsOne&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;@OrOneEqualsOne&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;VBA Stomping 是一种可以绕过反病毒检测恶意文档生成技术，它最初由 Vesselin Bontchev 博士引起我们的注意（见&lt;a href=&quot;https://github.com/bontchev/pcodedmp&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;此处&lt;/a&gt;）。VBA stomping 是指销毁 Microsoft Office 文档中的 VBA 源代码，只留下文档文件中称为 p-code 的宏代码的编译版本。在这种情况下，仅基于VBA源代码的恶意文档检测会失败。在这篇博文中，我们将详细演示 VBA stomping 并介绍一些其他技术。&lt;/p&gt;
    
    </summary>
    
      <category term="安全技术" scheme="http://zanderchang.github.io/categories/%E5%AE%89%E5%85%A8%E6%8A%80%E6%9C%AF/"/>
    
    
      <category term="外文翻译" scheme="http://zanderchang.github.io/tags/%E5%A4%96%E6%96%87%E7%BF%BB%E8%AF%91/"/>
    
      <category term="VBA" scheme="http://zanderchang.github.io/tags/VBA/"/>
    
      <category term="安全技术" scheme="http://zanderchang.github.io/tags/%E5%AE%89%E5%85%A8%E6%8A%80%E6%9C%AF/"/>
    
  </entry>
  
  <entry>
    <title>AFL 及其相关拓展项目总结</title>
    <link href="http://zanderchang.github.io/2019/04/29/AFL%E5%8F%8A%E5%85%B6%E7%9B%B8%E5%85%B3%E6%8B%93%E5%B1%95%E9%A1%B9%E7%9B%AE%E6%80%BB%E7%BB%93/"/>
    <id>http://zanderchang.github.io/2019/04/29/AFL及其相关拓展项目总结/</id>
    <published>2019-04-29T06:44:42.000Z</published>
    <updated>2019-05-06T02:57:13.632Z</updated>
    
    <content type="html"><![CDATA[<p><em>原文标题 Zoo AFL (Apr 24, 2019)</em></p><p>作者：<a href="https://habr.com/en/users/d1g1/" target="_blank" rel="noopener">@d1g1</a></p><p>本文主要讨论的不是经典 AFL 本身，而是为其设计的工具以及对其本身做出的改进，我们认为这些改进可以显著提高模糊测试的质量。如果你想知道如何提高 AFL 效率以及如何更快地找到更多漏洞，请继续阅读！</p><a id="more"></a><h1 id="AFL-是什么，它有什么用？"><a href="#AFL-是什么，它有什么用？" class="headerlink" title="AFL 是什么，它有什么用？"></a>AFL 是什么，它有什么用？</h1><p>AFL是一个覆盖导向或基于反馈的模糊测试工具，关于这些概念的更多信息可以在参考这篇论文 <a href="https://arxiv.org/abs/1812.00140" target="_blank" rel="noopener">Fuzzing: Art, Science, and Engineering</a>。总结一下 AFL：</p><ul><li>它改进可执行文件以了解它如何影响覆盖范围</li><li>变异输入数据以最大化覆盖范围</li><li>重复上一步以找到程序崩溃的位置</li><li>通过实践证明它非常有效</li><li>它易于使用</li></ul><p>如果你不清楚 AFL 是什么的话，这里有一些资源帮助你上手和理解：</p><ul><li><a href="http://lcamtuf.coredump.cx/afl/" target="_blank" rel="noopener">项目主页</a></li><li><a href="https://github.com/ThalesIgnite/afl-training" target="_blank" rel="noopener">AFL 练习</a></li><li><a href="https://gitlab.com/wolframroesler/afl-demo" target="_blank" rel="noopener">AFL Demo</a> — 使用 AFL fuzz C++ 程序</li><li><a href="https://github.com/mrash/afl-cve" target="_blank" rel="noopener">AFL 发现的漏洞</a> —（截至 2017）</li><li>在<a href="https://tunnelshade.in/blog/2018/01/afl-internals-compile-time-instrumentation/" target="_blank" rel="noopener">这里</a>你可以读到关于 AFL 在构建过程中添加到程序中的内容</li><li><a href="https://copyninja.info/blog/afl-and-network-programs.html" target="_blank" rel="noopener">一些</a>关于 fuzz 网络程序的技巧</li></ul><p>在写这篇文章时 AFL 的最新版本是<a href="http://lcamtuf.coredump.cx/afl/" target="_blank" rel="noopener">2.52b</a>。随着时间的推移，一些分支正在被纳入到 AFL 主分支。接下来我们会列举几个有用的附件工具。</p><p><strong>关于 Rode0day 竞赛</strong></p><p><a href="https://rode0day.mit.edu/" target="_blank" rel="noopener">Rode0day</a> 竞赛每月举办一次，参赛者要在花费更少时间的情况下在预先制作的语料库（无论是否获取源代码）中找到最多的漏洞。就其本质而言，Rode0day 是 AFL 不同分支之间的争斗。</p><p>一些AFL用户<a href="https://groups.google.com/forum/#!topic/afl-users/6NTPAkK7JEk" target="_blank" rel="noopener">指出</a>，AFL 的作者 Michal Zalewski 显然已经放弃该项目， 因为上次的改进日期还得追溯到 2017 年 11 月 5 日。这可能与他离开 Google 并开展一些新项目有关。 因此用户开始为最新的版本 2.52b 制作新的<a href="https://github.com/vanhauser-thc/afl-patches" target="_blank" rel="noopener">补丁</a>)。</p><p>AFL 还有不同的变体和衍生物，允许模糊测试 Python，Go，Rust，OCaml，GCJ Java，内核系统调用，甚至整个虚拟机。</p><p><strong>AFL 对其它语言的支持</strong></p><ul><li><a href="https://github.com/jwilk/python-afl" target="_blank" rel="noopener">python-afl</a> — Python</li><li><a href="https://github.com/rust-fuzz/afl.rs" target="_blank" rel="noopener">afl.rs</a> — Rust</li><li><a href="https://github.com/connor4312/js-fuzz" target="_blank" rel="noopener">afl-fuzz-js</a> — Javascript</li><li><a href="https://github.com/Barro/java-afl" target="_blank" rel="noopener">java-afl</a> — Java</li><li><a href="https://github.com/isstac/kelinci" target="_blank" rel="noopener">kelinci</a> — Java（<a href="https://www.modzero.ch/modlog/archives/2018/09/20/java_bugs_with_and_without_fuzzing/index.html" target="_blank" rel="noopener">相关文章</a>）</li><li><a href="https://github.com/cretz/javan-warty-pig" target="_blank" rel="noopener">javan-warty-pig</a> — JVM</li><li><a href="https://github.com/Proteas/afl-swift" target="_blank" rel="noopener">afl-swift</a> — Swift</li><li><a href="https://github.com/kayceesrk/ocamlopt-afl" target="_blank" rel="noopener">ocamlopt-afl</a> — OCaml</li><li><a href="https://github.com/Metalnem/sharpfuzz" target="_blank" rel="noopener">sharpfuzz</a> — .Net</li></ul><h1 id="AFL-附属工具"><a href="#AFL-附属工具" class="headerlink" title="AFL 附属工具"></a>AFL 附属工具</h1><p>本节我们收集了 AFL 的各种脚本和工具，并将它们分为几类：</p><h2 id="崩溃处理"><a href="#崩溃处理" class="headerlink" title="崩溃处理"></a>崩溃处理</h2><ul><li><a href="https://gitlab.com/rc0r/afl-utils" target="_blank" rel="noopener">afl-utils</a> — 自动处理/分析崩溃和减少测试用例数量</li><li><a href="https://github.com/floyd-fuh/afl-crash-analyzer" target="_blank" rel="noopener">afl-crash-analyzer</a> — AFL 崩溃分析程序</li><li><a href="https://github.com/ThePatrickStar/fuzzer-utils" target="_blank" rel="noopener">fuzzer-utils</a> — 分析结果</li><li><a href="https://github.com/Ayrx/atriage" target="_blank" rel="noopener">atriage</a> — 简单的分类工具</li><li><a href="https://github.com/kcwu/afl-kit" target="_blank" rel="noopener">afl-kit</a> — Python 实现的 afl-cmin</li><li><a href="https://github.com/d33tah/aflize" target="_blank" rel="noopener">AFLize</a> — 生成适合 AFL 的 debian 包的构建</li><li><a href="https://github.com/FoRTE-Research/afl-fid" target="_blank" rel="noopener">afl-fid</a> — 处理输入数据</li></ul><h2 id="代码覆盖率"><a href="#代码覆盖率" class="headerlink" title="代码覆盖率"></a>代码覆盖率</h2><ul><li><a href="https://github.com/mrash/afl-cov" target="_blank" rel="noopener">afl-cov</a> — 提供人性化的覆盖率数据</li><li><a href="https://github.com/Barro/count-afl-calls" target="_blank" rel="noopener">count-afl-calls</a> — 使用脚本计算二进制文件中的插桩块数，评估其占比</li><li><a href="https://github.com/bshastry/afl-sancov" target="_blank" rel="noopener">afl-sancov</a> — 类似于 afl-cov 但使用 clang的消毒机制（sanitizer）</li><li><a href="https://github.com/Cisco-Talos/covnavi" target="_blank" rel="noopener">covnavi</a> — 思科 Talos 开发的用于代码覆盖和分析的脚本</li><li><a href="https://gitlab.com/laf-intel/laf-llvm-pass/tree/master" target="_blank" rel="noopener">LAF LLVM Passes</a> — 类似于 AFL 的补丁集合，可以改进代码使其更容易找到分支</li></ul><h2 id="一些用于最小化测试用例的脚本"><a href="#一些用于最小化测试用例的脚本" class="headerlink" title="一些用于最小化测试用例的脚本"></a>一些用于最小化测试用例的脚本</h2><ul><li><a href="https://github.com/ilsani/afl-pytmin" target="_blank" rel="noopener">afl-pytmin</a> — 一个 afl-tmin 的包装器，它试图通过使用多 CPU 内核来加速最小化测试用例的过程</li><li><a href="https://github.com/MarkusTeufelberger/afl-ddmin-mod" target="_blank" rel="noopener">afl-ddmin-mod</a> — 基于 ddmin 算法的 afl-tmin</li><li><a href="https://github.com/googleprojectzero/halfempty" target="_blank" rel="noopener">halfempty</a> — 来自 Tavis Ormandy 基于并行化的快速最小化测试用例的程序</li></ul><h2 id="分布式执行"><a href="#分布式执行" class="headerlink" title="分布式执行"></a>分布式执行</h2><ul><li><a href="https://github.com/MartijnB/disfuzz-afl" target="_blank" rel="noopener">disfuzz-afl</a> — AFL 的分布式模糊测试</li><li><a href="https://github.com/quantumvm/AFLDFF" target="_blank" rel="noopener">AFLDFF</a> — AFL 分布式模糊测试框架</li><li><a href="https://github.com/bnagy/afl-launch" target="_blank" rel="noopener">afl-launch</a> — AFL 多实例执行</li><li><a href="https://github.com/afl-mothership/afl-mothership" target="_blank" rel="noopener">afl-mothership</a> — AWS 云上多同步 AFL 的管理和执行</li><li><a href="https://github.com/abhisek/afl-in-the-cloud" target="_blank" rel="noopener">afl-in-the-cloud</a> — 另一个在 AWS 中运行 AFL 的脚本</li><li><a href="https://github.com/clvang000/VU_BSc_project" target="_blank" rel="noopener">VU_BSc_project</a> — 使用 libFuzzer 和 AFL 对开源库进行模糊测试</li></ul><p>最近有一篇文章 <a href="https://gamozolabs.github.io/fuzzing/2018/09/16/scaling_afl.html" target="_blank" rel="noopener">Scaling AFL to a 256 thread machine</a>。</p><h2 id="部署，管理，监控，报告"><a href="#部署，管理，监控，报告" class="headerlink" title="部署，管理，监控，报告"></a>部署，管理，监控，报告</h2><ul><li><a href="https://github.com/shellphish/afl-other-arch" target="_blank" rel="noopener">afl-other-arch</a> — 用于添加对各种非x86体系结构的支持</li><li><a href="https://github.com/bnagy/afl-trivia" target="_blank" rel="noopener">afl-trivia</a> — 简化 AFL 管理的脚本</li><li><a href="https://github.com/reflare/afl-monitor" target="_blank" rel="noopener">afl-monitor</a> — AFL 监控脚本</li><li><a href="https://github.com/zx1340/afl-manager" target="_blank" rel="noopener">afl-manager</a> — Python 实现的用于管理 multi-afl 的 Web 服务器</li><li><a href="https://hub.docker.com/r/moflow/afl-tools/" target="_blank" rel="noopener">afl-tools</a> — 带有 afl-latest, afl-dyninst 和 Triforce-afl 的 docker 镜像</li><li><a href="https://github.com/block8437/afl-remote" target="_blank" rel="noopener">afl-remote</a> — 远程管理 AFL 实例的 Web 服务器</li></ul><h2 id="包装器（wrapper）"><a href="#包装器（wrapper）" class="headerlink" title="包装器（wrapper）"></a>包装器（wrapper）</h2><ul><li><a href="https://github.com/angr/phuzzer" target="_blank" rel="noopener">phuzzer</a> - 基于 Python 与 AFL 交互</li></ul><h1 id="AFL-改进"><a href="#AFL-改进" class="headerlink" title="AFL 改进"></a>AFL 改进</h1><p>AFL 对漏洞研究人员和模糊测试本身产生了巨大的影响，他们开始根据原始 AFL 进行改进。在不同的情况下，与原始AFL相比，每个改进都有其自身的优缺点。</p><p>几乎所有改进版本可以在 <a href="https://hub.docker.com" target="_blank" rel="noopener">hub.docker.com</a> 找到。</p><p>目的是什么？</p><ul><li><p>提高速度并/或提高代码覆盖率</p><ul><li>算法</li><li>环境<ul><li>操作系统</li><li>硬件</li></ul></li></ul></li><li><p>无源码环境</p><ul><li>代码模拟</li><li>代码插桩<ul><li>静态</li><li>动态</li></ul></li></ul></li></ul><h2 id="AFL-默认模式"><a href="#AFL-默认模式" class="headerlink" title="AFL 默认模式"></a>AFL 默认模式</h2><p>在继续研究 AFL 的不同改进和分支之前，我们必须讨论两种重要的模式，这些模式在过去也有过改进但最终被合并。它们是 Syzygy 和 QEMU。</p><h3 id="Syzygy-模式-—-instrument-exe-中工作的模式"><a href="#Syzygy-模式-—-instrument-exe-中工作的模式" class="headerlink" title="Syzygy 模式 — instrument.exe 中工作的模式"></a><a href="https://doar-e.github.io/blog/2017/08/05/binary-rewriting-with-syzygy/" target="_blank" rel="noopener">Syzygy</a> 模式 — instrument.exe 中工作的模式</h3><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">instrument<span class="selector-class">.exe</span> --mode=afl --input-image=test<span class="selector-class">.exe</span> --output-image=test<span class="selector-class">.instr</span><span class="selector-class">.exe</span></span><br></pre></td></tr></table></figure><p>Syzygy 允许使用 AFL 静态重写 PE32 二进制文件，但需要符号和额外环境的才能识别 WinAFL 内核。</p><h3 id="QEMU-模式-—-QEMU-下的工作方式见-Internals-of-AFL-fuzzer-—-QEMU-Instrumentation"><a href="#QEMU-模式-—-QEMU-下的工作方式见-Internals-of-AFL-fuzzer-—-QEMU-Instrumentation" class="headerlink" title="QEMU 模式 — QEMU 下的工作方式见 Internals of AFL fuzzer — QEMU Instrumentation"></a>QEMU 模式 — QEMU 下的工作方式见 <a href="https://tunnelshade.in/blog/2018/02/afl-internals-qemu-instrumentation/" target="_blank" rel="noopener">Internals of AFL fuzzer — QEMU Instrumentation</a></h3><p>使用 QEMU 实现对二进制文件的支持在版本 1.31b 中被添加到 AFL 处理流程上游。AFL QEMU 模式使用 QEMU tcg（一个微小的代码生成器）二进制转换引擎的二进制插桩功能。为此，AFL 有一个 QEMU 的构建脚本，它提取特定版本 QEMU（2.10.0）的源代码，将它们放到几个小的补丁上，并为指定架构构建。然后，创建名为 afl-qemu-trace 的文件，该文件实际上是 QEMU 用户模式模拟（仅可执行ELF文件）的文件。因此，AFL 可以使用不同体系架构（QEMU 支持）中 elf 二进制文件的反馈进行模糊测试。此外，还有许多 AFL 工具，比如从显示器上获取有关当前会话的信息，以及 afl-analyze 等高级内容。但同时 AFL 也会受到 QEMU 的局限， 比如如果工具链使用硬件 SoC 功能构建文件（该功能启动二进制并且 QEMU 不支持），则只要有特定指令或使用特定 MMIO，模糊测试就会中断。</p><p><a href="https://abiondo.me/2018/09/21/improving-afl-qemu-mode/" target="_blank" rel="noopener">这里</a>是 QEMU 模式的另一个有趣的分支，其中使用TCG代码插桩和缓存，速度提高了3-4倍。</p><h2 id="分支"><a href="#分支" class="headerlink" title="分支"></a>分支</h2><p>AFL 的分支首先与经典 AFL 算法的变化和改进有关。</p><ul><li><a href="https://github.com/wmliang/pe-afl" target="_blank" rel="noopener">pe-afl</a> — 对在 Windows 操作系统中没有源代码的 PE 文件进行模糊测试的改进。fuzzer 使用 IDA Pro 分析目标程序，并生成用于接下来静态插桩的信息。然后用 AFL 对插桩后的程序进行模糊测试。</li><li><a href="https://github.com/arizvisa/afl-cygwin" target="_blank" rel="noopener">afl-cygwin</a> — 尝试使用 Cygwin 将经典 AFL 移植到Windows。然而它有很多错误且很慢，作者已经放弃了开发</li><li><a href="https://github.com/mboehme/aflfast" target="_blank" rel="noopener">AFLFast</a>（使用 Power Schedule 扩展 AFL） — 最早的 AFL 分支之一。它增加了启发式功能，允许它在短时间内通过更多路径</li><li><a href="https://github.com/carolemieux/afl-rb" target="_blank" rel="noopener">FairFuzz</a> — AFL 的扩展，针对难抵达的分支</li><li><a href="https://github.com/aflgo/aflgo" target="_blank" rel="noopener">AFLGo</a> — AFL 的扩展，获取代码的某些部分而不是完整的程序覆盖，可用于测试补丁或新添加的代码片段</li><li><a href="https://github.com/carolemieux/perffuzz" target="_blank" rel="noopener">PerfFuzz</a> — AFL 的扩展，用于查找可能显着减慢程序速度的测试用例</li><li><a href="https://github.com/mboehme/pythia" target="_blank" rel="noopener">Pythia</a> — AFL 的扩展，预测找到新路径的难度</li><li><a href="https://github.com/AngoraFuzzer/Angora" target="_blank" rel="noopener">Angora</a> — 最新的 fuzzer 之一，用 Rust 语言编写，使用新的策略进行变异并增加覆盖范围。</li><li><a href="https://github.com/Dongdongshe/neuzz" target="_blank" rel="noopener">Neuzz</a> — 用神经网络进行模糊测试</li><li><a href="https://github.com/FoRTE-Research/UnTracer-AFL" target="_blank" rel="noopener">UnTracer-AFL</a> — 将 AFl 与 UnTracer 集成以进行有效跟踪</li><li><a href="https://github.com/sslab-gatech/qsym/" target="_blank" rel="noopener">Qsym</a> — 针对混合模糊测试的实用混合执行（concolic execution）引擎。从本质上讲，它是一个符号执行引擎（基本组件作为 intel pin 的插件来实现），它与 AFL 一起执行混合模糊测试。这是基于反馈的模糊测试演变的一个阶段，需要单独讨论。它的主要优点是可以相对快速地进行混合执行。这是由于本机执行命令而没有代码、快照和一些启发式的中间表示。它使用旧版本的Intel pin（由于 libz3 和其他 DBT 之间的支持问题），目前支持ELF x86 和 x86_64 架构</li><li><a href="https://github.com/zhunki/Superion" target="_blank" rel="noopener">Superion</a> — 灰盒模糊测试器，其优点是除了插桩程序外，它还使用 ANTLR 语法获取输入数据的规范，然后在此语法的帮助下执行变异</li><li><a href="https://github.com/aflsmart/aflsmart" target="_blank" rel="noopener">AFLSmart</a> — 另一个灰盒模糊测试器，它以 Peach 使用的格式获得输入数据的规范</li></ul><p>有许多研究论文致力于实现 AFL 被改进的新方法和模糊测试技术。由于它们只有白皮书，所以我们甚至没有提到这些。如果你愿意，你可以搜索它们。例如最新的一些有 <a href="http://chao.100871.net/papers/oakland18.pdf" target="_blank" rel="noopener">CollAFL: Path Sensitive Fuzzing</a>、<a href="https://arxiv.org/pdf/1807.00182.pdf" target="_blank" rel="noopener">EnFuzz</a>、<a href="https://i.blackhat.com/asia-19/Fri-March-29/bh-asia-Dominiak-Efficient-Approach-to-Fuzzing-Interpreters-wp.pdf" target="_blank" rel="noopener">Efficient approach to fuzzing interpreters</a>、<a href="https://arxiv.org/pdf/1811.08973.pdf" target="_blank" rel="noopener">ML</a> 用于 AFL 等。</p><h2 id="基于-QEMU-的改进"><a href="#基于-QEMU-的改进" class="headerlink" title="基于 QEMU 的改进"></a>基于 QEMU 的改进</h2><ul><li><a href="https://github.com/nccgroup/TriforceAFL" target="_blank" rel="noopener">TriforceAFL</a> — AFL/QEMU对系统完全模拟来进行模糊测试。是由 nccgroup 提供的一个分支。它允许在 QEMU 模式下对整个操作系统进行模糊测试。它是通过一个特殊指令（aflCall (0f 24)）（在 QEMU x64 CPU 中添加）实现的。然而它不再受支持; 支持 AFL 的最后一个版本是2.06b</li><li><a href="https://github.com/nccgroup/TriforceLinuxSyscallFuzzer" target="_blank" rel="noopener">TriforceLinuxSyscallFuzzer</a> — Linux 系统调用的模糊测试</li><li><a href="https://github.com/kanglictf/afl-qai" target="_blank" rel="noopener">afl-qai</a> — 一个带有 QEMU 增强插桩（qai）的小型演示项目。</li></ul><h2 id="基于-KLEE-的改进"><a href="#基于-KLEE-的改进" class="headerlink" title="基于 KLEE 的改进"></a>基于 KLEE 的改进</h2><ul><li><a href="https://github.com/julieeen/kleefl" target="_blank" rel="noopener">kleefl</a> — 通过符号执行生成测试用例（在大程序上非常慢）</li></ul><h2 id="基于-Unicorn-的改进"><a href="#基于-Unicorn-的改进" class="headerlink" title="基于 Unicorn 的改进"></a>基于 Unicorn 的改进</h2><ul><li><a href="https://github.com/Battelle/afl-unicorn" target="_blank" rel="noopener">afl-unicorn</a> — 允许通过在 <a href="https://www.unicorn-engine.org/" target="_blank" rel="noopener">Unicorn Engine</a> 上模拟代码片段进行模糊测试</li></ul><p>我们在实践中成功使用了 AFL 的这种改进，由于目标是在 SOC 上执行的某个 RTOS（real-time operating system，实时操作系统）的代码区域，因此我们无法使用QEMU模式。在我们没有源代码的情况下（我们无法构建用于解析器分析的独立二进制文件）并且程序不直接获取输入数据（例如，数据被加密或是如在 CGC 二进制文件中的信号样本），然后我们可以逆向并找到所谓的位置函数，函数中中数据以便于模糊测试器使用的格式进行处理。这是 AFL 最普遍/通用的修改，即它允许模糊测试任何东西。它独立于架构、源、输入数据格式和二进制格式（例如裸机，只是来自控制器内存的代码片段）。研究人员首先检查这个二进制文件并编写一个模糊测试器，它在解析过程的输入端模拟状态。显然与 AFL 不同，这需要对二进制进行一定的检查。对于裸机固件，如 Wi-FI 或基带，我们需要记住一些缺点：</p><ul><li>我们必须本地处理控制和的检查</li><li>模糊测试器的状态是保存在内存转储中的内存状态，这可以防止模糊测试器进入某些路径</li><li>动态内存调用没有消毒，但它可以手动实现，它将取决于 RTOS（必须进行研究）</li><li>未模拟任务内 RTOS 交互，这也可能阻止寻找某些路径</li></ul><p>使用这种修改的例子 <a href="https://hackernoon.com/afl-unicorn-fuzzing-arbitrary-binary-code-563ca28936bf" target="_blank" rel="noopener">afl-unicorn: Fuzzing Arbitrary Binary Code</a> 和 <a href="https://hackernoon.com/afl-unicorn-part-2-fuzzing-the-unfuzzable-bea8de3540a5" target="_blank" rel="noopener">afl-unicorn: Part 2 — Fuzzing the ‘Unfuzzable’</a>。</p><p>在我们继续基于动态二进制检测（DBI）框架进行修改之前，不要忘记这些框架的最快速度由 DynamoRIO，Dynlnst 以及 PIN 实现。</p><h2 id="基于-PIN-的改进"><a href="#基于-PIN-的改进" class="headerlink" title="基于 PIN 的改进"></a>基于 PIN 的改进</h2><ul><li><a href="https://github.com/mothran/aflpin" target="_blank" rel="noopener">aflpin</a> — 采用 Intel PIN 插桩的 AFL</li><li><a href="https://github.com/spinpx/afl_pin_mode" target="_blank" rel="noopener">afl_pin_mode</a> — 另一个采用 Intel PIN 插桩的 AFL</li><li><a href="https://github.com/vanhauser-thc/afl-pin" target="_blank" rel="noopener">afl-pin</a> — 采用 PINtool 的 AFL</li><li><a href="https://github.com/carlosgprado/NaFl" target="_blank" rel="noopener">NaFl</a> — AFL 模糊测试器的克隆（基本核心）</li><li><a href="https://github.com/houcy/PinAFL" target="_blank" rel="noopener">PinAFL</a> — 该工具的作者试图将 AFL 移植到 Windows，以便对已编译的二进制文件进行模糊测试。该项目好像是为了好玩而在一夜之间完成的且未进一步发展。存储库里没有源文件，只有已编译的二进制文件和启动指令。我们不知道它基于哪个版本的 AFL，它只支持 32 位应用程序。</li></ul><p>正如我们所看到的，AFL 有许多不同的修改，但它们在现实生活中并不是非常有用。</p><h2 id="基于-Dyninst-的改进"><a href="#基于-Dyninst-的改进" class="headerlink" title="基于 Dyninst 的改进"></a>基于 Dyninst 的改进</h2><p><a href="https://github.com/talos-vulndev/afl-dyninst" target="_blank" rel="noopener">afl-dyninst</a> — AFL + Dyninst == AFL 黑盒模糊测试。这个版本的特点是首先使用 Duninst 对一个研究过的程序（没有源代码）进行静态插桩（静态二进制插桩，静态二进制重写），然后使用经典 AFL 进行模糊测试，AFL 会认为程序是用 afl-gcc/afl-g++/afl-as 构建的。因此，它在没有源代码的情况下以非常好的生产率工作：与本地编译相比，经典 AFL 是 0.25 倍的速度。与QEMU相比，它具有显着优势：它允许动态链接库的插桩，而 QEMU 只能插桩与库静态链接的基本可执行文件。然而现在它只支持 Linux，如果增加对于 Windows 支持，则需要对 Dyninst 本身进行更改，这个工作<a href="https://github.com/dyninst/dyninst/issues/120" target="_blank" rel="noopener">正在进行</a>中。</p><p>还有另一个<a href="https://github.com/vanhauser-thc/afl-dyninst" target="_blank" rel="noopener">分支</a>具有提升的速度和某些功能（AARCH64 和 PPC 架构的支持）。</p><h2 id="基于-DynamoRIO-的改进"><a href="#基于-DynamoRIO-的改进" class="headerlink" title="基于 DynamoRIO 的改进"></a>基于 DynamoRIO 的改进</h2><ul><li><a href="https://github.com/mxmssh/drAFL" target="_blank" rel="noopener">drAFL</a> — AFl + DynamoRIO – 在 Linux 上没有源代码的模糊测试</li><li><a href="https://github.com/atrosinenko/afl-dr" target="_blank" rel="noopener">afl-dr</a> — 基于 DynamoRIO 的另一种实现，在 <a href="https://habrahabr.ru/post/332076/" target="_blank" rel="noopener">Habr</a> 上有很好的描述</li><li><a href="https://github.com/vanhauser-thc/afl-dynamorio" target="_blank" rel="noopener">afl-dynamorio</a> — 来自 vanhauser-thc 的修改。 据他所说：“当正常的 afl-dyninst 能够使二进制崩溃并且 QEMU 模式 -Q 无法执行时，用 DynamoRIO 运行 AFL。”它支持 ARM 和 AARCH64。DynamoRIO 比 QEMU 慢大约 10 倍，比 dyninst 慢 25 倍，但比 Pintool 快 10 倍。</li><li><a href="https://github.com/ivanfratric/winafl" target="_blank" rel="noopener">WinAFL</a> — 最著名的 AFL分支。（DynamoRIO，也是 syzygy 模式）。这个修改的出现只是时间问题，因为许多人想在 Windows 上尝试 AFL 并将其应用于没有源代码的应用程序。目前，这个工具正在被积极改进，尽管 AFL 代码库（撰写本文时为 2.43b）相对过时，但它都有助于发现多个漏洞（CVE-2016-7212，CVE-2017-0073，CVE- 2017-0190，CVE-2017-11816）。Google Zero Project 团队和 MSRC 漏洞与解决团队的专家正在参与此项目，因此我们希望它能够得到进一步的发展。开发人员使用动态插桩（基于DynamoRIO）而不是编译时插桩，这显着减慢了被分析软件的执行速度，但产生的开销（加倍）与二进制模式下的经典 AFL 相当。他们还解决了快速启动过程的问题，称其为持续模糊测试模式；他们选择对函数进行模糊测试（通过文件内部的偏移或导出表中存在的函数名称）并对其进行插桩，以便可以在循环中调用它，从而启动多个输入数据样本而无需重新启动程序。最近发表的一篇<a href="https://research.checkpoint.com/50-adobe-cves-in-50-days/" target="_blank" rel="noopener">文章</a>描述了作者如何使用 WinAFL 在约 50 天内发现约 50 个漏洞。发布之后很快 WinAFL 就支持 Intel PT（Processor Tracing，处理器跟踪）模式，在这里可以找到<a href="https://github.com/googleprojectzero/winafl/blob/master/readme_pt.md" target="_blank" rel="noopener">细节信息</a>。</li></ul><p>专业读者可能注意到这些改进使用了除了Frida之外的各种流行的插桩框架。 唯一提到 Frida 与 AFL 的结合使用是在 <a href="http://wpage.unina.it/roberto.natella/papers/natella_androidfuzzing_issre2017.pdf" target="_blank" rel="noopener">Chizpurfle: A Gray-Box Android Fuzzer for Vendor Service Customizations</a> 中找到的。 带 Frida 的 AFL 版本非常有用，因为 Frida 支持多种 RISC 架构。</p><p>许多研究者也期待由 Capstone，Unicorn 和 Keystone 的创建者发布的 DBI Scopio 框架。基于这个框架，作者已经创建了一个模糊测试器（Darko），据他们说能够成功地使用它来对嵌入式设备进行模糊测试。有关这方面的更多信息，请参阅 <a href="https://conference.hitb.org/hitbsecconf2018pek/materials/D2T1%20-%20Finding%200days%20in%20Embedded%20Systems%20with%20Code%20Coverage%20Guided%20Fuzzing%20-%20Dr%20Quynh%20and%20Kai%20Jern%20Lau.pdf" target="_blank" rel="noopener">Digging Deep: Finding 0days in Embedded Systems with Code Coverage Guided Fuzzing</a>。</p><h2 id="基于处理器硬件特征的改进"><a href="#基于处理器硬件特征的改进" class="headerlink" title="基于处理器硬件特征的改进"></a>基于处理器硬件特征的改进</h2><p>关于支持处理器硬件功能的 AFL 修改方面，首先，它允许对内核代码进行模糊测试；其次，它允许在没有源代码的情况下更快地对应用程序进行模糊测试。</p><p>当然，谈到处理器硬件功能，我们最感兴趣的是 <a href="https://software.intel.com/en-us/node/721535" target="_blank" rel="noopener">Intel PT</a>。从第 6 代处理器开始（大约自2015年起）支持该功能。 因此，为了能够使用下面列出的模糊测试器，我们需要一个支持 Intel PT 的处理器。</p><ul><li><a href="https://github.com/intelpt/winafl-intelpt" target="_blank" rel="noopener">WinAFL-IntelPT</a> — 使用 Intel PT 而不是 DynamoRIO 的第三方 WinAFL。</li><li><a href="https://github.com/hunter-ht-2018/ptfuzzer" target="_blank" rel="noopener">ptfuzzer</a> - 使用 Intel PT 对二进制进行黑盒测试。</li><li><a href="https://github.com/RUB-SysSec/kAFL" target="_blank" rel="noopener">kAFL</a> — 一个旨在解决独立于操作系统的模糊测试内核时的覆盖率导向问题的学术项目。 它通过使用管理程序（hypervisor）和 Intel PT 来解决该问题。有关它的更多信息可以在白皮书 <a href="https://www.usenix.org/system/files/conference/usenixsecurity17/sec17-schumilo.pdf" target="_blank" rel="noopener">kAFL: Hardware-Assisted Feedback Fuzzing for OS Kernels</a> 中找到。</li></ul><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>如你所见，研究人员仍在积极地改进 AFL，而且 AFL 还存在进行实验和创新的空间，我们也可以创建一个我们自己的实用有趣的 AFL。</p><p>感谢阅读，祝大家 Fuzz 顺利。</p><p><a href="https://habr.com/ru/company/dsec/blog/449134/" target="_blank" rel="noopener">原文链接</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;em&gt;原文标题 Zoo AFL (Apr 24, 2019)&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;作者：&lt;a href=&quot;https://habr.com/en/users/d1g1/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;@d1g1&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;本文主要讨论的不是经典 AFL 本身，而是为其设计的工具以及对其本身做出的改进，我们认为这些改进可以显著提高模糊测试的质量。如果你想知道如何提高 AFL 效率以及如何更快地找到更多漏洞，请继续阅读！&lt;/p&gt;
    
    </summary>
    
      <category term="漏洞挖掘" scheme="http://zanderchang.github.io/categories/%E6%BC%8F%E6%B4%9E%E6%8C%96%E6%8E%98/"/>
    
    
      <category term="外文翻译" scheme="http://zanderchang.github.io/tags/%E5%A4%96%E6%96%87%E7%BF%BB%E8%AF%91/"/>
    
      <category term="漏洞挖掘" scheme="http://zanderchang.github.io/tags/%E6%BC%8F%E6%B4%9E%E6%8C%96%E6%8E%98/"/>
    
      <category term="AFL" scheme="http://zanderchang.github.io/tags/AFL/"/>
    
  </entry>
  
</feed>
